# Доска Задач (Kanban Board)

Добро пожаловать в трекер задач проекта **RUMPEL RUST**.
Здесь агенты отслеживают прогресс по задачам.

## 📝 Backlog (Бэклог - Ожидает выполнения)
- [ ] GPU-generated PackedVoxelQuad moving-world lifecycle: camera-driven region set, stable region slot reuse/eviction, per-region active masks, and generated-path GPU cull without CPU quad uploads
- [ ] Packed GPU-cull compact/count acceptance на backend с `MULTI_DRAW_INDIRECT_COUNT`; на Metal/macOS keep opt-in
- [ ] Far-field SVO/SVDAG или raymarched terrain для очень дальней прорисовки
- [ ] GPU wind uniform для шейдерной анимации листвы
- [ ] Binary greedy meshing для full voxel chunks через occupancy/face masks
- [ ] Генерация мира с помощью шума Перлина (горы, долины)
- [ ] Механика установки и разрушения блоков
- [ ] Материалы/текстуры `bevy_voxel_world` из `BlockRegistry` вместо временного index mapping
- [ ] Адаптер редактирования блоков через `WorldPos`/`LocalBlockPos` поверх `VoxelWorld`
- [ ] Lua API для событий блоков (`on_block_break`, `on_block_place`)
- [ ] Lua API для рецептов и предметов
- [ ] Расширить Lua worldgen с spawn chunk на deterministic multi-chunk generation без повторяющихся построек
- [ ] Packed voxel quad rendering / vertex pulling с custom material pipeline как visual/material parity baseline, не performance target
- [ ] Проработка базового GDD (Крафт, список руд)
- [ ] Подбор текстур (Art Department)

## ⏳ In Progress (В работе)
- [ ] Настройка пайплайна Большой Студии (CI/CD, GitHub, RAG update)

## 🔍 In Review (На проверке)
- [ ] Method A split display A/B на macOS GUI: `just compare-split-display` (baseline window Core3d vs `RUMPEL_SPLIT_DISPLAY=1` offscreen Core3d + Core2d blit). Критерий принятия: снижение `frames_ge_25ms` / `worst_render_prepare_windows_us` без регрессии `avg_raw_fps`; при провале — переход к Method C (custom wgpu present)
- [ ] Проверка FPS при быстром полете после async mesh streaming

## ✅ Done (Готово)
- [x] Packed streaming membership sync больше не создает локальный `HashSet` каждый center-change frame: `PackedQuadStreamingState` хранит переиспользуемый `regions_to_sync` scratch, очищает его в начале кадра и синхронизирует region flags из него. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render streaming_grid_radius_targets --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated render prepare больше не вызывает `clone()` для planned regions при rebuild: `PreparedPackedGpuGeneratedRegion` стал `Copy`, а job/params construction берет регион обычной копией из `planned_regions`. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated region cache retain больше не строит `target_keys: HashSet`: non-skip update path сортирует переиспользуемый `loaded_region_keys` scratch и делает `binary_search` при `region_cache.entries.retain`, сохраняя stable-target early-out без дополнительной работы. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render generated_region_cache --lib`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated arena allocation planner получил sorted-input hot path: `prepare_packed_gpu_generated_draw` вызывает `plan_gpu_generated_arena_allocations_sorted`, поэтому generated prepare больше не делает `requests.to_vec()` и повторный `sort_by_key` для уже ordered batches. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render plan_gpu_generated_arena_allocations --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed arena allocation prepare теперь переиспользует scratch `HashMap`/`Vec` внутри `PreparedPackedQuadIndirectDraw`: stable и compacted allocation plans пишутся in-place, а accepted plan переходит в `arena.allocations` через `swap`, оставляя старый map как scratch для следующего кадра. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_plan_stable_arena_allocations --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed arena reallocation больше не пересоздаёт `dirty_batch_keys`: prepare clear/reserve/extend переиспользует Vec, когда reallocation помечает все extracted batches full-dirty. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_arena_growth_from_zero --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo test -p rumpel_render test_prune_dirty_ranges_keeps_unconfirmed_generations --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed prepare переиспользует один sorted batch order для allocation planning и indirect build: `sort_packed_batch_order` заполняет `PreparedPackedQuadIndirectDraw::batch_order`, а `plan_stable_arena_allocations_for_order` убирает дополнительный `collect::<Vec<_>>()`/sort в hot prepare path. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_stable_offsets_for_sorted_input --lib`, `cargo test -p rumpel_render test_plan_stable_arena_allocations_reuses_offsets --lib`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed dirty upload decisions больше не создают локальный `HashSet`: `PreparedPackedQuadIndirectDraw::dirty_batch_key_set` clear/reserve/fill переиспользуется для full-dirty lookup перед upload. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_prune_dirty_ranges_keeps_unconfirmed_generations --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed prepared-batch eviction больше не сканирует extracted batches для каждого prepared batch: `PreparedPackedQuadIndirectDraw::active_batch_keys` переиспользуется как HashSet, prepare clear/reserve/fill строит active key set за один проход, а `retain` делает O(1) lookup. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_evict_last_chunk_removes_empty_region_batch --lib`, `cargo test -p rumpel_render test_active_chunk_ranges_emit_only_active_indirect_commands --lib`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed dirty-range upload больше не клонирует previous arena allocations: prepare читает previous allocation напрямую из `arena.allocations` до финального move нового allocation map. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_prune_dirty_ranges_keeps_unconfirmed_generations --lib`, `cargo test -p rumpel_render test_plan_stable_arena_allocations_reuses_offsets --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed arena allocation update больше не клонирует весь allocation HashMap: `new_allocations` остаётся источником lookup до конца prepare и move-ится в `arena.allocations` после последнего использования. Static proof: `cargo test -p rumpel_render test_plan_stable_arena_allocations_reuses_offsets --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed indirect prepare переиспользует staging buffers: `PreparedPackedQuadIndirectDraw` хранит `batch_order`, `commands`, `command_metadata` и `params_staging`; prepare clear/reserve/fill строит sorted indirect stream без локальных staging Vec и без полного replacement ресурса в конце кадра. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo test -p rumpel_render test_active_chunk_ranges_emit_only_active_indirect_commands --lib`, `cargo test -p rumpel_render test_collect_visible_indirect_commands_preserves_draw_command_order --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed CPU-source GPU cull metadata upload переиспользует `PreparedPackedQuadGpuCull::metadata_scratch`: prepare path больше не собирает временный `collect::<Vec<_>>()` перед `write_buffer`. Static proof: `cargo test -p rumpel_render test_gpu_cull_metadata_from_indirect_command --lib`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Packed indirect visibility accounting больше не аллоцирует `HashSet`: renderer считает visible batches по переходам сгруппированного `batch_key` stream в CPU-visible selection, estimate path и multi-indirect metrics path; тест покрывает duplicate face-range commands одного batch. Static proof: `cargo test -p rumpel_render test_collect_visible_indirect_commands_preserves_draw_command_order --lib`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo test -p rumpel_render packed_gpu_generation_visibility_estimate --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Generated GPU cull metadata upload переиспользует persistent scratch: `PreparedPackedQuadGpuCull` хранит `metadata_scratch`, а prepare clear/reserve/fill пишет metadata без временного `collect::<Vec<_>>()` при rebuild сигнатуры. Static proof: `cargo test -p rumpel_render packed_gpu_generation_cull --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Generated indirect CPU fallback visibility стал streaming: render path больше не собирает `visible_regions.collect::<Vec<_>>()`, а считает visible regions/quads и вызывает `draw_indirect` за один проход по prepared regions. Static proof: `cargo test -p rumpel_render packed_gpu_generation_visibility_estimate --lib`, `cargo test -p rumpel_render packed_gpu_generation_cull --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Generated render prepare переиспользует scratch Vec для planned regions: `PackedGpuGenerationBuffers` хранит `planned_regions`, prepare clear/reserve заполняет его повторно, а `prepared.regions.clone_from(...)` сохраняет prepared-side allocation без локального `Vec::with_capacity` на каждый non-reused prepare. Static proof: `cargo test -p rumpel_render packed_gpu_generation_prepared --lib`, `cargo test -p rumpel_render packed_gpu_generation_visibility_estimate --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Generated region builder больше не делает post-pass по appended chunk columns для local offset: добавлен `append_surface_gpu_generation_columns_for_chunk_with_local_offset`, который пишет offset сразу при packing. Добавлен parity test на offset без repacking. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render generated_region_cache --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Generated region update фиксирует `edit_store_generation` один раз на frame: target/cache entries/invalidation logs теперь используют один snapshot вместо повторных `edit_store.generation()` calls. Static proof: `cargo test -p rumpel_render generated_region_cache --lib`, `cargo test -p rumpel_render gpu_generation_contract --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Generated region miss loop больше не пересчитывает `source_chunk_count` и `expected_column_count` на каждый region: frame-constant capacity math вынесен перед active-region loop. Static proof: `cargo test -p rumpel_render generated_region_cache --lib`, `cargo test -p rumpel_render packed_gpu_generation_region_radius_for_view --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Batched neighbor-height sampling получил no-edits specialization: baseline generated path возвращает 4 deterministic noise heights напрямую и не заходит в общий edit-aware sampler с `has_edits` branch на каждый neighbor. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] `SurfaceColumnSource` кэширует world origin чанка (`world_origin_x/z`): traversal и batched neighbor sampler больше не умножают `chunk_pos * CHUNK_SIZE` на каждую column. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] `surface_neighbor_heights` теперь принимает `SurfaceColumnSource` напрямую: append call-site не пересобирает context/edit/perlin/has_edits аргументы, а neighbor sampler использует тот же source object, что и traversal. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated column append path теперь пакует 4 neighbor heights через `surface_neighbor_heights`: base world X/Z считается один раз на column, а одиночный `surface_neighbor_height` переиспользует общий low-level sampler для wall/tests. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Surface column traversal получил private `SurfaceColumnSource`: параметры chunk/context/cell/material/edit/perlin/has_edits теперь передаются одним compact source object, `has_edits` считается один раз на chunk и больше не дублируется внутри traversal. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Generated GPU surface columns получили edit-aware parity test: тест поднимает deterministic surface cell через `WorldBlockEdit`, считает ожидаемый `terrain_surface_cell_sample_with_edits` и проверяет, что GPU column packing пишет ту же высоту/top material. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Generated surface neighbor-height sampling получил baseline fast path: при пустом `WorldEditStore` CPU/GPU column builders не создают neighbor `ChunkPos` и не делают per-column `chunk_revision` lookups, а сразу используют deterministic noise height. Edit-aware path остается включенным через `has_edits`. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] Surface/GPU-generated column builders переиспользуют один `Perlin` context на chunk: CPU surface path больше не создает noise отдельно для column sampling и wall neighbors, а GPU append path использует тот же context для surface samples и neighbor heights. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated column append path больше не строит промежуточный `SurfacePackedColumn` Vec: общий `for_each_surface_packed_column_for_chunk` обходит cell grid один раз, CPU builder остается wrapper-ом, а GPU append сразу конвертирует каждый surface column в `PackedGpuSurfaceColumn`. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated cache-miss builder убрал per-source-chunk GPU column Vec: добавлен `append_surface_gpu_generation_columns_for_chunk`, старый `build_*` остался wrapper-ом, а region builder append-ит chunk columns прямо в preallocated region Vec и корректирует offsets на newly appended slice. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated cache-miss path preallocates region column storage: new generated cache entries create the region `columns` Vec with `source_chunk_count * packed_gpu_generation_columns_per_chunk(cell_size)` capacity, avoiding growth reallocations while preserving the shared source contract. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated main-world update переиспользует builder Vec для generated batches: `PackedGpuGenerationRegionScratch::generated_batches` собирает и сортирует batches, а при signature change swap-ится с `gpu_batches.batches`; scratch очищается до выхода, чтобы не держать старые `Arc` columns. Static proof: `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo test -p rumpel_render packed_gpu_generation_batch_summary --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated main-world update получил reusable region scratch: loaded region keys, active region triples и target-key `HashSet` теперь живут в `PackedGpuGenerationRegionScratch` и переиспользуют capacity между кадрами; stable-target early-out по-прежнему выходит до target-key set. Static proof: `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated render prepare переиспользует CPU scratch buffers для rebuild upload payload: `PackedGpuGenerationBuffers` хранит allocation requests, generation jobs, source columns и draw params между кадрами, поэтому rebuild path больше не создает новые staging `Vec` для каждого upload. Static proof: `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo test -p rumpel_render packed_gpu_generation_batch_summary --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated render prepare больше не реконструирует temporary allocation `HashMap` из prepared regions на rebuild path: planner читает существующий `PackedQuadGpuArena::allocations`, а затем arena atomically получает новый map. Это сохраняет free-slot reuse semantics и убирает лишний per-rebuild collect. Static proof: `cargo test -p rumpel_render plan_gpu_generated_arena_allocations --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated render prepare получил producer-side batch summary: generated update сохраняет total columns/max quads/max columns/source chunks/invalid count рядом с `batch_signature`, render prepare больше не сканирует batches для validation/totals перед steady skip, а changed detection сравнивает полный signature вместо key/generation scan. Static proof: `cargo test -p rumpel_render packed_gpu_generation_batch_summary --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated render prepare получил O(1) steady batch signature match: producer хранит `batch_signature` для отсортированного generated batch set, а render prepare сравнивает signature/count/arena generation вместо per-region scan в `matches_batches`. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated render prepare убрал per-frame `Vec<&Batch>` + sort: main-world producer сортирует generated batches по key при rebuild, а render prepare берет ordered slice напрямую и не аллоцирует/сортирует refs на steady path. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated cull visible estimate стал direct region pass: generated path больше не строит metadata `Vec` и не использует generic `HashSet` для visible batches при dispatch; он считает visible commands/quads напрямую по prepared regions. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_visibility_estimate --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated cull dispatch path кеширует bind group в prepare stage: `PreparedPackedQuadGpuCull` хранит generated cull bind group и пересоздает его только при смене buffers/source signature, а render node использует cached group с CPU fallback. Это убирает per-dispatch bind-group creation на generated cull hot path. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated cull skip path больше не аллоцирует CPU metadata `Vec`: `PackedQuadGpuCullSource::Generated` хранит borrowed regions, а cull metadata для CPU-side visible estimate собирается только после проверки dispatch reuse. Это убирает лишнюю allocation на steady `generated_cull_dispatch_skipped=true` кадрах. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated cull node получил steady-view dispatch reuse: cull dispatch signature учитывает source/metadata/config signatures и view transform, поэтому generated path может переиспользовать прошлый culled indirect output без compute pass, если камера и source не изменились. `profile sample`, `profile worst_packed`, summary и generated compare reports показывают `generated_cull_dispatch_skipped`. Static proof: `cargo fmt --all`, `bash -n` для compare/profile scripts, synthetic summary check, `cargo test -p rumpel_render packed_gpu_generation_cull --lib`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `just verify`.
- [x] GPU-generated render/cull cache telemetry выведена в profile contract: `profile sample`, `profile worst_packed`, summary и generated compare reports показывают `generated_prepare_skipped`, `generated_cull_metadata_uploaded`, `generated_cull_config_uploaded`, чтобы A/B логи доказывали steady render-prepare skip и отсутствие лишних cull uploads. Static proof: `cargo fmt --all`, `bash -n` для compare/profile scripts, synthetic summary check, `cargo test -p rumpel_render packed_gpu_generation_cull_signatures --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `just verify`.
- [x] GPU-generated cull metadata signature теперь хранится в `PreparedPackedGpuGeneratedDraw`: steady render-prepare skip больше не хеширует bounds/max-quads по всем regions заново, а передает cached signature в generated cull prepare. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_cull_signatures --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `just verify`.
- [x] GPU-generated cull prepare перестал переписывать static metadata/config каждый steady frame: `PreparedPackedQuadGpuCull` хранит metadata/config signatures, а generated cull path пишет buffers только при смене region bounds/max quads/command count/compact mode или при пересоздании buffer. Это убирает лишний `Vec` metadata + `write_buffer` на steady render-prepare skip path. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_cull_signatures --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `just verify`.
- [x] GPU-generated render-prepare получил ранний skip для уже подготовленных batches: `PreparedPackedGpuGeneratedDraw::matches_batches` позволяет steady path пропустить allocation planning, planned-region rebuild, buffer writes и bind-group rebuild, если prepared regions, arena generation и extracted batches уже совпадают. Per-view GPU cull prepare при этом продолжает обновляться. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `just verify`.
- [x] GPU-generated arena allocation planner убрал лишний churn: `plan_gpu_generated_arena_allocations` больше не строит `HashSet` active keys и не клонирует весь `existing_allocations`; он использует sorted request keys, binary search для active existing и копирует только нужные allocation entries, сохраняя free-slot reuse для evicted regions. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render plan_gpu_generated_arena_allocations --lib`, `just verify`.
- [x] GPU-generated skipped update path больше не строит `HashSet` loaded region keys: update сначала собирает compact `Vec` для loaded keys и active signature, а `HashSet` для cache retain создается только если early-out не сработал и нужен rebuild/reuse pass. Это убирает лишнюю allocation/hash cost на steady и unchanged-active moving frames. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `just verify`.
- [x] GPU-generated active-region test переведен с per-chunk scan на O(1) rectangle-vs-circle check: `region_has_active_chunks` теперь ищет ближайший chunk region AABB к camera center и сравнивает с radius. Это уменьшает CPU cost нового active-region signature path на каждом кадре, особенно при больших region sizes. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render region_has_active_chunks --lib`, `just verify`.
- [x] GPU-generated moving-camera early-out расширен до active-region signature: update теперь собирает loaded/active region keys, строит `active_region_hash`, и пропускает cache walk/source rebuild, если loaded window/source/edit generation и active region set не изменились даже при смене camera chunk. Это снижает CPU spikes при плавном полете внутри той же region window без риска недогрузить новые активные регионы. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `just verify`.
- [x] GPU-generated region cache lifecycle стал измеримым: packed stats/profile reports теперь показывают `generated_cache_hits`, `generated_cache_misses`, `generated_cache_invalidated`, `generated_cache_evicted` рядом с `generated_update_us/skipped`. Это отделяет steady-camera skip от moving-camera cache reuse/rebuild/eviction и готовит следующую итерацию по снижению spikes на полете. Static proof: `cargo fmt --all`, `bash -n` для compare/profile scripts, synthetic summary check, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `just verify`.
- [x] GPU-generated stable-target early-out получил explicit telemetry: packed stats теперь содержат `generated_update_us` и `generated_update_skipped`, `profile sample`/`profile worst_packed`/summary/visual+headless reports показывают эти поля. Это позволяет доказать, что stable camera path реально пропускает planning и сколько CPU времени остается. Static proof: `cargo fmt --check`, `bash -n` для compare/profile scripts, synthetic summary check, `just verify`.
- [x] GPU-generated region update получил stable-target early-out: `PackedGpuGenerationBatches` хранит `PackedGpuGenerationTarget` (camera chunk, region window, view radius, source contract generation, edit-store generation). Если target не изменился и batches уже есть, main-world update пропускает повторный region planning/column-cache walk, но обновляет region mask metrics. Это снижает CPU work на стабильной камере и готовит moving-window eviction/reuse. Static proof: `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `just verify`.
- [x] Generated-region lifecycle metrics выведены в profile contract: `profile sample` теперь пишет `packed_generated_regions_loaded/active/visible`, `profile worst_packed` пишет `generated_regions_loaded/active/visible`, summary показывает эти поля, а visual/headless generated compare reports и acceptance требуют positive loaded/active/visible regions для generated variant. Это готовит измеримый переход к camera-driven generated window eviction/reuse. Static proof: `cargo fmt --check`, `bash -n` для compare/profile scripts, synthetic summary check, `just verify`.
- [x] Generated headless A/B profile harness добавлен: `scripts/compare_generated_headless_profile.sh` сравнивает CPU-built `packed` против `RUMPEL_PACKED_GPU_GENERATION=1` + GPU cull без window/swapchain visual path, пишет per-variant runner logs/env/profile summaries и report с FPS/frame-tail/draw-mode/visible/uploaded/indirect/GPU-cull metrics. `scripts/profile_packed_headless.sh` теперь явно экспортирует и пишет в state `RUMPEL_PACKED_GPU_GENERATION`, `RUMPEL_PACKED_GPU_GENERATION_REGION_RADIUS`, `RUMPEL_PACKED_GPU_CULL`. Static proof: `bash -n scripts/compare_generated_headless_profile.sh scripts/profile_packed_headless.sh`; full workspace proof: `just verify`.
- [x] Packed profiling contract расширен для generated/indirect анализа: `profile worst_packed` теперь печатает `draw_mode`, `indirect_draw_commands`, draw calls/items, GPU-cull enabled/input/visible/quads/count/compact flags и CPU-visible compact flag. `scripts/summarize_profile_log.sh` выводит эти поля в `worst_packed`, а generated visual harness читает acceptance metrics из worst packed stats вместо первого sample. Static proof: `cargo fmt --check`, `bash -n scripts/compare_generated_visual_profile.sh scripts/summarize_profile_log.sh`, synthetic summary check, `just verify`.
- [x] Generated visual/profile compare harness получил fail-fast acceptance checks: CPU variant не может случайно запускать `gpu-generated`, generated variant обязан логировать `packed_draw_mode=gpu-generated`, `packed_uploaded_quads=0`, `packed_gpu_cull_enabled=true`, positive `packed_gpu_cull_input_commands`, `packed_gpu_cull_est_visible_commands` и `packed_gpu_cull_est_visible_quads`. Это превращает visual compare из “картинки рядом” в проверку реального GPU-generated path. Static proof: `bash -n scripts/compare_generated_visual_profile.sh`; full workspace proof: `just verify`.
- [x] Generated visual/profile compare harness добавлен: `scripts/compare_generated_visual_profile.sh` строит release client один раз и сравнивает `packed` CPU-built quads против `RUMPEL_PACKED_GPU_GENERATION=1` + GPU cull на camera presets `horizon`, `ridge`, `beach`, сохраняя stdout/stderr/profile summaries/screenshots и report с FPS/frame-tail/visible/uploaded/indirect/GPU-cull metrics. macOS GUI launchers теперь прокидывают `RUMPEL_PACKED_GPU_GENERATION` и `RUMPEL_PACKED_GPU_GENERATION_REGION_RADIUS`, чтобы Terminal-запуск реально включал generated path. Static proof: `bash -n scripts/compare_generated_visual_profile.sh scripts/run_client_macos_gui.sh scripts/prepare_client_macos_app.sh`; full workspace proof: `just verify`.
- [x] Golden terrain source samples зафиксированы для generated parity: `rumpel_world::world_gen` теперь проверяет hard-coded raw height, shell height, cell height и top material по нескольким deterministic регионам/cell sizes, включая beach/sand sample `(-179, -512)` и multi-cell sample `(512, 384, 4x3)`. Это ловит случайную смену seed/noise/kernel/beach/material contract до того, как GPU-generated columns начнут визуально расходиться с CPU packed path. Static proof: `cargo fmt --check`, `cargo test -p rumpel_world terrain_surface_sampling_matches_golden_regions -- --nocapture`, `just verify`.
- [x] GPU-generated PackedVoxelQuad world-source parity закрыта статически: deterministic terrain sampling для generated columns вынесен в `rumpel_world::world_gen` как общий surface contract (`terrain_perlin`, shell/cell height sampling, top material, wall layers), CPU packed columns и GPU-generated column buffer теперь строятся через один source builder, а `surface_streaming` использует тот же top/wall material contract без смены текущей raw-height сетки. Generated-region cache invalidation переключен на `terrain_surface_contract_version()` и учитывает material contract; существующий `WorldEditStore` path preserved с per-region edit generation invalidation. Lua `world_gen.lua` post-pass не включался в generated path: оставлен bounded spawn-chunk special case до отдельного измеримого bridge. Static proof: `cargo test -p rumpel_world`, `cargo test -p rumpel_render generated_surface_columns -- --nocapture`, `cargo test -p rumpel_render generated_region_cache -- --nocapture`, `cargo test -p rumpel_render` (`128` passed), `just verify`. Runtime visual proof attempted with `RUMPEL_PACKED_GPU_GENERATION=1`, high camera, and capture, but current Codex macOS context is blocked: Terminal launch returns `kLSNoExecutableErr`, direct shell run reports no Metal adapter before Bevy startup.
- [x] GPU-generated PackedVoxelQuad frustum cull path закрыт статически: generated indirect commands теперь подготавливают `PackedQuadCullCommandMetadata` и проходят через общий `packed_quad_cull.wgsl` compute node вместо render-node-only CPU region filtering. Metal-safe путь остается fixed-count `multi_draw_indirect` по GPU-culled output buffer с zeroed invisible commands; `MULTI_DRAW_INDIRECT_COUNT` используется только при backend feature support через existing compact/count branch. Metrics сохраняют input commands (`gpu_cull_input_commands` / `indirect_draw_commands`), estimated visible commands (`gpu_cull_est_visible_commands` / `generated_regions_visible`) и visible quads (`gpu_cull_est_visible_quads` / `visible_quads`). Static proof: `cargo test -p rumpel_render packed_quad_renderer::tests --lib`, `cargo test -p rumpel_render packed --lib` (`92` filtered packed tests), `cargo check -p rumpel_render -p rumpel_client -p rumpel_debug`, `cargo clippy -p rumpel_render -p rumpel_client -p rumpel_debug --all-targets --all-features -- -D warnings`, `just verify` (`125` rumpel_render tests). Runtime GUI proof attempted with generated mode, but current Codex LaunchServices context returns `kLSNoExecutableErr` for Terminal/app `open`, and direct shell run exits `78` because `wgpu` exposes no Metal adapter; no validation error was observed because the client could not reach GPU startup in this environment.
- [x] GPU-generated PackedVoxelQuad source-contract invalidation закрыта: generated-region cache больше не держит hardcoded `generation=1`; `PackedGpuGenerationCacheContract` включает region size, normalized cell size, derived LOD, terrain palette IDs, sand/top material ID, `rumpel_world::world_gen::terrain_generation_contract_version()` и relevant `BlockRegistry::material_contract_version_for_blocks(...)`. Same-key stale cache entries теперь не переиспользуются при смене source/material contract, а render prepare получает новую generation и перезапускает GPU generation без CPU quad uploads. Static proof: `cargo test -p rumpel_blocks --lib`, `cargo test -p rumpel_world world_gen --lib`, `cargo test -p rumpel_render gpu_generation --lib`, `cargo test -p rumpel_render packed --lib` (`86` passed), `cargo check -p rumpel_blocks -p rumpel_world -p rumpel_render`, `cargo clippy -p rumpel_blocks -p rumpel_world -p rumpel_render --all-targets --all-features -- -D warnings`. Runtime proof `.ai_tasks/rumpel_client_packed_20260602_181004.stdout.log` status `0`, `autopilot=true`, `generation=2252600004647481695`, `7` generated target updates, `packed_uploaded_quads=0`, `max_pending_builds=0`, `max_pending_region_rebuilds=0`, validation-clean stderr.
- [x] GPU-generated PackedVoxelQuad multi-region dispatch foundation: `RUMPEL_PACKED_GPU_GENERATION=1` теперь строит grid generated regions через `RUMPEL_PACKED_GPU_GENERATION_REGION_RADIUS` (default `1`), WGSL использует `jobs[]` ABI с per-region column/output/counter/draw-command offsets, `generate` dispatch идет по `(max_columns, region_count)`, `finalize` пишет массив indirect commands, а render pass делает один `multi_draw_indirect` по generated command buffer. Повторная compute generation пропускается, если region generations/arena layout не изменились. Static proof: `cargo check -p rumpel_render -p rumpel_debug`, `cargo clippy -p rumpel_render -p rumpel_client -p rumpel_debug --all-targets --all-features -- -D warnings`, focused tests `packed_gpu_generation` (`7` passed), `packed_quad_renderer::tests` (`6` passed), `packed_quad_pipeline::tests` (`42` passed). Runtime proof `.ai_tasks/rumpel_client_packed_20260602_162742.stdout.log` status `0`, stderr validation-clean: `batches=9`, `columns=36864`, `packed_chunks_active=144`, `packed_draw_mode=gpu-generated`, `packed_indirect_draw_commands=9`, `packed_render_draw_calls=1`, `packed_uploaded_quads=0`, `arena_used_quads=479232`, `arena_slot_quads=589824`; visual proof `.ai_tasks/rumpel_client_packed_20260602_162742.png` shows horizon/multi-level mountains/valley without transparent wall, HUD `Cmds: 9, Vis: 9, Draws: 1`.
- [x] GPU-generated PackedVoxelQuad runtime proof slice: `RUMPEL_PACKED_GPU_GENERATION=1` builds compact column inputs for one centered packed region, dispatches `generate` + `finalize` compute passes in the render graph, writes quads into `PackedQuadGpuArena`, emits one GPU indirect draw command, and renders the GPU-generated path while skipping CPU packed streaming/uploads. Static proof: `cargo check -p rumpel_render -p rumpel_client -p rumpel_debug`, `cargo clippy -p rumpel_render -p rumpel_client -p rumpel_debug --all-targets --all-features -- -D warnings`, `cargo test -p rumpel_render packed --lib` (`78` passed), `cargo test -p rumpel_render packed_gpu_generation --lib` (`6` passed), `cargo fmt -p rumpel_render -p rumpel_client -p rumpel_debug -- --check`, `git diff --check` on touched files. Runtime proof `.ai_tasks/rumpel_client_packed_20260602_155825.stdout.log` status `0`, stderr validation-clean: `packed_draw_mode=gpu-generated`, `packed_visible_quads=53248`, `arena_used_quads=53248`, `packed_uploaded_quads=0`, `packed_indirect_draw_commands=1`, `packed_render_draw_calls=1`, `avg_raw_fps=91.9`.
- [x] GPU-generated PackedVoxelQuad foundation: host-side `PackedVoxelQuad` ABI/capacity contract, bounded column-input WGSL generator, dropped-counter path, and WGSL validation test
- [x] Packed tombstone fragmentation / dirty-subrange retention telemetry закрыта: packed profile logs и `scripts/summarize_profile_log.sh` теперь показывают `packed_chunk_ranges`, resident/tombstone ranges, tombstone capacity, dirty subrange count/quads и arena slot/used quads. Долгий pre-cleanup proof `.ai_tasks/rumpel_client_packed_20260602_145921.stdout.log` status `0`, stderr validation-clean, `measured_duration=180.0s`, `ready_status=ready`, показал проблему не в tombstones, а в stale dirty metadata: `max_tombstone_ranges=165`, `max_tombstone_capacity_quads=69666`, но `max_dirty_ranges=976`, `max_dirty_range_quads=433711`, `max_pending_region_rebuilds=0`. Cleanup реализован через render-world confirmed batch generations: main-world удаляет только dirty ranges с generation уже подтвержденной GPU upload planning, без эвристического TTL и без потери еще не загруженных subranges. Static proof: `cargo fmt -p rumpel_render -p rumpel_client -- --check`, `cargo check -p rumpel_render -p rumpel_client`, `cargo clippy -p rumpel_render -p rumpel_client --all-targets --all-features -- -D warnings`, `cargo test -p rumpel_render packed --lib` (`72` passed), `bash -n scripts/summarize_profile_log.sh`. Acceptance `.ai_tasks/rumpel_client_packed_20260602_150906.stdout.log` status `0`, stderr validation-clean, `measured_duration=60.0s`, `ready_status=ready`: dirty metadata bounded to `max_dirty_ranges=2`, `max_dirty_range_quads=955`, while `max_pending_region_rebuilds=0`, `max_uploaded_quads=10800`, `max_tombstone_ranges=148`, `max_tombstone_capacity_quads=65915`; remaining worst frames still dominated by macOS/Bevy `prepare_windows`, not packed render/cleanup.
- [x] Packed region per-chunk slot free-list / tombstone eviction закрыт: despawned chunk ranges теперь превращаются в resident=false tombstone slots без bump region generation и без постановки full region rebuild/upload, последний chunk удаляет пустой region batch, а новые completed chunks переиспользуют подходящий tombstone slot с точечной `PackedQuadDirtyRange` upload вместо перепаковки всего региона. Indirect command staging и active sync игнорируют non-resident ranges, сохраняя strict active draw masks. Static proof: `cargo fmt -p rumpel_render -- --check`, `cargo check -p rumpel_render`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `cargo test -p rumpel_render packed_quad_pipeline::tests --lib` (`41` passed), `cargo test -p rumpel_render packed --lib` (`71` passed). Runtime proof `.ai_tasks/rumpel_client_packed_20260602_142909.stdout.log` status `0`, stderr validation-clean: `ready_status=ready`, measured `avg_raw_fps=101.7`, `frames_ge_25ms=4`, `packed_hotpath_measured max_visible_quads=92828`, `max_render_us=51`, `max_cpu_cmds=193`, `max_uploaded_quads=13583`, `max_pending_region_rebuilds=0`; remaining worst-frame tail again sits in macOS/Bevy `prepare_windows`, not packed render/upload. Visual proof `.ai_tasks/rumpel_client_packed_20260602_143233.png` status `0` shows normal horizon/multi-level mountains without transparent holes, HUD `Chunks 797`, `Packed Q 91036/366790`, `Mode: indirect (Cmds: 797, Vis: 198, Draws: 1, CPU: 67us)`.
- [x] Per-chunk draw ranges/masks для persistent packed region buffers закрыты: `PackedQuadBatch` теперь хранит `PackedQuadChunkRange { chunk_key, start_quads, len_quads, active }`, active/inactive membership меняет только range flags без bump `generation`, а indirect command staging эмитит команды только для активных chunk ranges с per-chunk AABB. Deferred compaction стала range-preserving: compact выполняется внутри chunk ranges и не склеивает quads через границы чанков, чтобы inactive halo не мог попасть в активный draw. Static proof: `cargo fmt -p rumpel_render -- --check`, `cargo check -p rumpel_render`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `cargo test -p rumpel_render packed_quad_pipeline::tests --lib` (`38` passed), `cargo test -p rumpel_render packed --lib` (`68` passed). Runtime proof `.ai_tasks/rumpel_client_packed_20260602_140452.stdout.log` status `0`, stderr validation-clean: `ready_status=ready`, measured `avg_raw_fps=128.5`, `frames_ge_25ms=1`, `packed_hotpath_measured max_visible_quads=93189`, `max_render_us=57`, `max_cpu_cmds=195`, `max_uploaded_quads=17712`, `max_pending_region_rebuilds=4`; strict active draw set вернул visible-quads из halo-регресса `112008` обратно в прежнюю зону `~85k-94k`. Visual proof `.ai_tasks/rumpel_client_packed_20260602_140620.png` status `0` показывает нормальный горизонт/горы/долины без прозрачной стены, HUD `Chunks 797`, `Packed Q 91036/366790`, `Mode: indirect (Cmds: 797, Vis: 198, Draws: 1, CPU: 23us)`. Следующий performance lever: убрать remaining eviction rebuild/upload через per-chunk slot free-list внутри region arena.
- [x] Packed moving path получил persistent loaded-region cache вместо rebuild-on-membership flip: active/inactive chunk membership больше не ставит region rebuild в очередь, completed chunks добавляются в region batch пока остаются в loaded cache, а rebuild региона нужен только для реального despawn/remove. Это уменьшает measured upload churn при автополете без урезания дальности. Static proof: `cargo fmt -p rumpel_render -- --check`, `cargo check -p rumpel_render`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `cargo test -p rumpel_render packed_quad_pipeline::tests --lib`, `cargo test -p rumpel_render packed --lib`, `git diff --check`. Runtime proof: old moving baseline `.ai_tasks/rumpel_client_packed_20260602_125435.stdout.log` имел `avg_raw_fps=120.0`, measured `max_uploaded_quads=19244`, `max_uploaded_this_frame=4`, `max_pending_region_rebuilds=10`, `max_render_us=29`, `max_visible_quads=95200`. Warm proof после правки `.ai_tasks/rumpel_client_packed_20260602_133715.stdout.log` status `0`, stderr validation-clean: `avg_raw_fps=133.9`, `frames_ge_25ms=0`, measured `max_uploaded_quads=7643`, `max_uploaded_this_frame=2`, `max_pending_region_rebuilds=7`, `max_render_us=15`, `worst_packed render_us=15 upload=0`. Tradeoff: cache now draws loaded halo too, so measured `max_visible_quads` rose to `112008`; next precision step is per-chunk draw ranges/masks if strict active draw set is needed without reintroducing rebuild churn. Visual proof `.ai_tasks/rumpel_client_packed_20260602_133844.png` status `0` shows clean high-camera mountains/horizon with `Draws: 1`, `Vis: 64`, `Packed Q 93871/360949`.
- [x] Packed GPU-cull A/B на measured window закрыт для Apple M4/Metal: добавлен fair `just profile-packed-gpu-cull-stationary` без GPU timestamp readback и `packed_hotpath_measured` теперь показывает GPU-cull fields. CPU-visible baseline `.ai_tasks/rumpel_client_packed_20260602_132527.stdout.log` status `0`, stderr validation-clean: `packed_hotpath_measured max_prepare_us=296 max_render_us=53 max_gpu_cull_us=0 gpu_cull_enabled=0 cpu_visible_compact=1 max_cpu_cmds=64 max_uploaded_quads=0`. GPU-cull run `.ai_tasks/rumpel_client_packed_20260602_132623.stdout.log` status `0`, stderr validation-clean: `max_prepare_us=305 max_render_us=37 max_gpu_cull_us=26 max_gpu_cull_input_cmds=262 max_gpu_cull_visible_cmds=64 gpu_cull_enabled=1 gpu_cull_count_supported=0 gpu_cull_compact_enabled=0 cpu_visible_compact=1`. Вывод: на текущем Metal backend GPU-cull не заменяет CPU-visible compact, потому что `MULTI_DRAW_INDIRECT_COUNT` недоступен; держим `RUMPEL_PACKED_GPU_CULL=1` diagnostic/opt-in до proof на backend с count support.
- [x] Packed indirect hot-path audit закрыт без смены renderer path: `prepare_packed_quad_buffers` больше не клонирует cached `face_ranges` для unchanged region-generation, а `just profile-packed-stationary` и `scripts/summarize_profile_log.sh` получили measured-only rollup `packed_hotpath_measured`. Static proof: `cargo fmt -p rumpel_render -- --check`, `cargo check -p rumpel_render`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `cargo test -p rumpel_render packed --lib`, `bash -n scripts/summarize_profile_log.sh`, `git diff --check`. Runtime proof: warm stationary `.ai_tasks/rumpel_client_packed_20260602_131711.stdout.log` status `0`, stderr validation-clean, `avg_raw_fps=125.6`, worst frame `29.35ms` снова dominated by `prepare_windows=27258us`; measured packed path stayed tiny: `packed_hotpath_measured max_prepare_us=241 max_render_us=61 max_uploaded_quads=0 max_pending_builds=0`, `worst_packed prepare_us=177 render_us=16 upload=0 cpu_cmds=64`.
- [x] Maximum-performance renderer ownership + fresh baseline закреплены: главным FPS-кандидатом остается `RUMPEL_RENDER_MODE=packed` / GPU-driven indirect; `packed_material` используется как visual/material parity baseline, не как performance target. Visual proof одной обзорной камерой: `surface` `.ai_tasks/rumpel_client_surface_20260602_125021.png`, `packed` `.ai_tasks/rumpel_client_packed_20260602_125044.png`, `packed_material` `.ai_tasks/rumpel_client_packed_material_20260602_125113.png`, bounded `compute` `.ai_tasks/rumpel_client_compute_20260602_125142.png`. Profile proof: `surface` `.ai_tasks/rumpel_client_surface_20260602_125418.stdout.log` (`avg_raw_fps=83.8`, worst `25.14ms`, `frames_ge_25ms=1`), `packed` `.ai_tasks/rumpel_client_packed_20260602_125435.stdout.log` (`avg_raw_fps=120.0`, worst `20.75ms`, `frames_ge_25ms=0`, `Draws=1`, worst packed `visible_quads=82349`, `render_us=17`, `cpu_cmds=54`), `packed_material` `.ai_tasks/rumpel_client_packed_material_20260602_125453.stdout.log` (`avg_raw_fps=81.0`, worst `32.55ms`, `frames_ge_25ms=4`, `material_entities=62`), `compute` `.ai_tasks/rumpel_client_compute_20260602_125513.stdout.log` (`avg_raw_fps=84.7`, worst `39.36ms`, `frames_ge_25ms=3`, bounded `chunks=26`, `max_draw_calls=1`). Status files all `0`; stderr scan found no panic/error/wgpu validation matches.
- [x] GPU occlusion culling для terrain draw commands после frustum/count foundation: реализована временная GPU-окклюзия невидимой геометрии (temporal occlusion culling) для рендеринга чанков. Шейдер `compute_terrain_cull.wgsl` осуществляет проецирование AABB чанка в экранное пространство, оценку пиксельного охвата и ранний выход при попиксельном сравнении глубины с использованием Reverse-Z логики. Для предотвращения паник WGPU на первом кадре используется dummy-текстура глубины 1x1, а последующие кадры безопасно копируют буфер глубины препасса `ViewPrepassTextures` (с флагами `COPY_SRC` и `TEXTURE_BINDING`). Локальная телеметрия подтверждает отсечение до 58% draw-команд скрытых чанков без снижения визуального качества. Все 107 тестов проходят полностью, форматирование чистое через `just verify`. Proof: `.ai_tasks/rumpel_client_compute_20260602_092445.stderr.log`.
- [x] Compute direct cull получил feature-gated indirect-count compaction path: `RUMPEL_COMPUTE_DIRECT_GPU_CULL_COMPACT=1` включен по умолчанию, но реально активируется только при `RUMPEL_COMPUTE_DIRECT_GPU_CULL=1`, `multi-indirect` draw mode и backend feature `MULTI_DRAW_INDIRECT_COUNT`. В этой ветке cull shader делает atomic append видимых commands в плотный indirect buffer, reset-ит GPU count buffer и render node вызывает `multi_draw_indirect_count`; на Metal/macOS feature недоступен, поэтому default runtime остается validation-safe fixed-count zero-command path. Telemetry/summary теперь различают `cull_count_supported` и `cull_compact_enabled`, а macOS GUI launchers прокидывают `RUMPEL_COMPUTE_DIRECT_GPU_CULL_COMPACT`. Unit/static proof: `cargo test -p rumpel_render compute_direct --lib` проверяет gating, `cargo check -p rumpel_render` подтверждает Bevy/wgpu API. Runtime Metal proof `.ai_tasks/rumpel_client_compute_20260602_081439.stdout.log` и warm repeat `.ai_tasks/rumpel_client_compute_20260602_081516.stdout.log`: `queue_chunks=26`, `loaded=26`, `draw_modes="multi-indirect"`, `cull_enabled=1`, `cull_count_supported=0`, `cull_compact_enabled=0`, `max_cull_visible=12`, `max_cull_culled=12`, status `0`, stderr validation-clean
- [x] Compute direct terrain получил GPU-driven frustum culling для indirect terrain draw commands: `RUMPEL_COMPUTE_DIRECT_GPU_CULL=1` теперь включен по умолчанию внутри `RUMPEL_RENDER_MODE=compute`, cull compute pass пишет per-view culled indirect buffer, невидимые чанки превращаются в zero draw commands, а Metal/macOS path сохраняет fixed-count `multi_draw_indirect` без требования `MULTI_DRAW_INDIRECT_COUNT`. `RUMPEL_COMPUTE_DIRECT_GPU_CULL=0` оставлен для A/B и fallback. Telemetry и summary теперь показывают `cull_enabled`, candidates/visible/culled commands и `cull_culled_sum`; macOS GUI launchers прокидывают новый env. Unit/static proof: `cargo test -p rumpel_render compute_direct --lib`, `cargo check -p rumpel_render`, `cargo fmt -p rumpel_render`, `bash -n scripts/run_client_macos_gui.sh scripts/prepare_client_macos_app.sh scripts/summarize_profile_log.sh`. Runtime proof: cull-on stress `.ai_tasks/rumpel_client_compute_20260602_080155.stdout.log` и repeat `.ai_tasks/rumpel_client_compute_20260602_080507.stdout.log` оба дали `queue_chunks=26`, `loaded=26`, `draw_modes="multi-indirect"`, `max_draw_calls=1`, `max_indirect_commands=24`, `cull_enabled=1`, `max_cull_visible=12`, `max_cull_culled=12`, status `0` и validation-clean stderr. A/B cull-off `.ai_tasks/rumpel_client_compute_20260602_080428.stdout.log` дал `cull_enabled=0`, `max_chunks_drawn=24`, `max_indices_drawn=345936`, status `0`; оставшиеся worst-frame хвосты сидят в известном macOS/Bevy `prepare_windows` / `render_before_render_system` классе, не в compute cull validation
- [x] Compute MultiDrawIndirect radius/stress acceptance закрыт и стал default внутри compute direct renderer: `RUMPEL_RENDER_MODE=compute` теперь выбирает `multi_draw_indirect` по умолчанию при доступном `INDIRECT_FIRST_INSTANCE`, сохраняя `RUMPEL_COMPUTE_DIRECT_MULTI_INDIRECT=0` для loop-based `draw_indirect` fallback и `RUMPEL_COMPUTE_DIRECT_INDIRECT=0` для direct draw loop fallback. Кодовое изменение узкое: `VoxelComputeSettings::from_env` теперь читает `RUMPEL_COMPUTE_DIRECT_MULTI_INDIRECT` как default-on, а unit-тест draw-mode decision проверяет приоритет multi и fallback на indirect. Acceptance proof до смены default: radius-2 stress `.ai_tasks/rumpel_client_compute_20260602_074529.stdout.log` дал `queue_chunks=26`, `loaded=26`, `max_chunks_drawn=24`, `max_draw_calls=1`, `max_indirect_commands=24`, `draw_modes="multi-indirect"`, worst `10.14ms`, `frames_ge_16ms=0`, status `0`; stderr validation-clean. Strict repeat `.ai_tasks/rumpel_client_compute_20260602_074608.stdout.log` дал `matched=2`, `mismatch=0`, `readback_failed=0`, `max_draw_calls=1`, `draw_modes="multi-indirect"`, status `0`; один `27.64ms` render-schedule tail при counters оставлен как известный macOS/Bevy хвост вне compute parity, не как MDI validation failure. Post-default proof без env: `.ai_tasks/rumpel_client_compute_20260602_074945.stdout.log` подтвердил `draw_modes="multi-indirect"` и `max_draw_calls=1`, но после fresh release rebuild поймал известный `56.90ms` `render_before_render_system` хвост; теплый repeat `.ai_tasks/rumpel_client_compute_20260602_075029.stdout.log` подтвердил `draw_modes="multi-indirect"`, `max_draw_calls=1`, `max_indirect_commands=24`, status `0`, worst `22.91ms`, `frames_ge_25ms=0`
- [x] GPU compute direct renderer получил общий arena + indirect draw command foundation: direct mode больше не держит per-chunk terrain bind group, а пишет compute output в fixed slots внутри global vertex/index storage buffers; `compute_terrain_direct.wgsl` выбирает chunk params через `@builtin(instance_index)` / `first_instance`, params buffer хранит per-slot offsets, а render node ставит один terrain bind group и по умолчанию использует `draw_indirect` loop при поддержке `INDIRECT_FIRST_INSTANCE`. `RUMPEL_COMPUTE_DIRECT_MULTI_INDIRECT=1` включает opt-in `multi_draw_indirect`, а `RUMPEL_COMPUTE_DIRECT_INDIRECT=0` оставляет direct draw loop fallback. GUI launchers теперь прокидывают `RUMPEL_COMPUTE_DIRECT_RENDER`, `RUMPEL_COMPUTE_DIRECT_INDIRECT`, `RUMPEL_COMPUTE_DIRECT_MULTI_INDIRECT`; `scripts/summarize_profile_log.sh` показывает `max_arena_slots`, `max_indirect_commands`, `draw_modes`. Unit/static proof: `cargo test -p rumpel_render compute_ --lib` (`23` tests), `cargo check -p rumpel_render`, `cargo fmt -p rumpel_render -- --check`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `bash -n scripts/run_client_macos_gui.sh scripts/prepare_client_macos_app.sh scripts/summarize_profile_log.sh`, `git diff --check` по измененным compute/shader/script файлам. Strict proof `.ai_tasks/rumpel_client_compute_20260602_073924.stdout.log`: `matched=2`, `mismatch=0`, `readback_failed=0`, `draw_modes="indirect"`, `max_arena_slots=2`, `max_indirect_commands=2`, `max_draw_calls=2`, worst `10.35ms`, `frames_ge_16ms=0`, status `0`. Radius-2 stress proof `.ai_tasks/rumpel_client_compute_20260602_073956.stdout.log`: `queue_chunks=26`, `prepared=26`, `loaded=26`, `draw_modes="indirect"`, `max_arena_slots=32`, `max_indirect_commands=24`, `max_chunks_drawn=24`, `skipped_without_bind_group_sum=0`, worst `10.24ms`, `frames_ge_16ms=0`, status `0`. Opt-in multi proof `.ai_tasks/rumpel_client_compute_20260602_074133.stdout.log`: `draw_modes="multi-indirect"`, `max_draw_calls=1`, `max_indirect_commands=2`, parity clean and validation-clean stderr; measured run caught one known macOS render-tail spike (`51.75ms`) outside compute/direct pass, so multi-indirect remains opt-in pending radius/stress acceptance
- [x] GPU compute queue получил direct terrain render phase / vertex pulling без Bevy mesh copy-back: `RUMPEL_RENDER_MODE=compute` теперь по умолчанию использует `RUMPEL_COMPUTE_DIRECT_RENDER=1`, compute mesher пишет в GPU-owned `VoxelComputeOutputBuffers`, а новый `VoxelComputeDirectRenderNode` рисует terrain напрямую из storage vertex/index buffers через `compute_terrain_direct.wgsl` и per-chunk params. Compatibility path с GPU-to-GPU copy-back в Bevy mesh buffers сохранен как `RUMPEL_COMPUTE_DIRECT_RENDER=0` fallback. `scripts/summarize_profile_log.sh` теперь печатает `compute_direct=...`. Unit/static proof: `cargo test -p rumpel_render compute_ --lib` (`21` tests), `cargo check -p rumpel_render`, `cargo fmt -p rumpel_render -- --check`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `bash -n scripts/summarize_profile_log.sh`, `git diff --check` по измененным compute/script файлам. Strict proof `.ai_tasks/rumpel_client_compute_20260602_072056.stdout.log`: `matched=2`, `mismatch=0`, `attention=0`, `readback_failed=0`, lifecycle `loaded=2`, `owned_output_buffers_sum=2`, `compute_direct=max_chunks_drawn=2 max_draw_calls=2 max_indices_drawn=42864 skipped_without_bind_group_sum=0`, measured `6.0s`, worst `9.77ms`, `frames_ge_16ms=0`, status `0`. Radius-2 stress proof `.ai_tasks/rumpel_client_compute_20260602_072124.stdout.log`: `queue_chunks=26`, `prepared=26`, `loaded=26`, `owned_output_buffers_sum=26`, `compute_direct=max_chunks_drawn=24 max_draw_calls=24 max_indices_drawn=345936 skipped_without_bind_group_sum=0`, worst `10.20ms`, `frames_ge_16ms=0`, `frames_ge_25ms=0`, status `0`
- [x] GPU compute queue получил GPU-owned output buffer bridge: compute meshing теперь пишет vertex/index output не напрямую в Bevy `MeshAllocator`, а в owned render-world storage buffers (`VoxelComputeOutputBuffers`) с тем же WGSL ABI (`13` u32-words на вершину), после чего текущий compatibility path делает GPU-to-GPU copy-back в Bevy mesh buffers для существующего draw path. Lifecycle eviction чистит owned output buffers вместе с input buffers, а `voxel compute lifecycle summary` / `scripts/summarize_profile_log.sh` теперь показывают `owned_output_buffers_sum` и `owned_output_bytes_sum`. Unit proof: `cargo test -p rumpel_render compute_ --lib` (`19` tests, включая output layout и copy byte clamp). Static proof: `cargo check -p rumpel_render`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, `bash -n scripts/summarize_profile_log.sh`. Strict parity proof `.ai_tasks/rumpel_client_compute_20260602_070435.stdout.log`: `matched=2`, `mismatch=0`, `attention=0`, `readback_failed=0`, lifecycle `loaded=2`, `owned_output_buffers_sum=2`, `owned_output_bytes_sum=7602176`, status `0`, `frames_ge_16ms=0`. Radius-2 stress proof `.ai_tasks/rumpel_client_compute_20260602_070501.stdout.log`: `queue_chunks=26`, `prepared=26`, `loaded=26`, `owned_output_buffers_sum=26`, `owned_output_bytes_sum=98828288`, `readback_failed=0`, `mismatch=0`, `frames_ge_25ms=0`, status `0`
- [x] GPU compute queue получил persistent multi-chunk edit store foundation: `rumpel_world::chunk::WorldEditStore` сохраняет последние `WorldBlockEdit` по typed `WorldBlockEditKey`, ведет `generation`, умеет применять overlay к 32-блочным chunk layers, а `record_world_block_edits` пишет store telemetry `world block edits stored`. `rumpel_player` и `VoxelComputePlugin` инициализируют store, player-side record system запускается после текущих edit producers, а compute generation применяет store overlay к source chunks, central `SingleChunkExtract` seed и neighbor boundary chunks. `scripts/summarize_profile_log.sh` теперь печатает `world_edits=...`. Unit proof: `cargo test -p rumpel_world world_edit --lib` (`2` focused tests), `cargo test -p rumpel_player single_chunk_diff --lib` (`2` tests), `cargo test -p rumpel_render compute_ --lib` (`17` tests, включая source overlay и neighbor boundary overlay). Strict parity proof `.ai_tasks/rumpel_client_compute_20260602_065429.stdout.log`: `matched=2`, `mismatch=0`, `attention=0`, `readback_failed=0`, lifecycle `loaded=2`, `compute_edits=-`, `world_edits=-`, status `0`; measured run caught one known macOS `render_before_render_system` tail spike (`51.57ms`) unrelated to compute parity/readback
- [x] GPU compute queue получил player/world edit producer bridge: `rumpel_world::chunk::WorldBlockEdit` стал нейтральным typed message (`ChunkPos + LocalBlockPos + BlockId`), `rumpel_player` регистрирует message и отправляет diff-based edits из `SingleChunkExtract` для Lua `on_step_on`, block break/place, sand gravity и water spread, а `VoxelComputePlugin` bridge переводит `WorldBlockEdit` в `VoxelComputeBlockEdit` перед source-side apply. Compute setup теперь seed-ит central `SingleChunkExtract` из parity chunk `ChunkPos { x: 0, z: 0 } / y_base=0`, чтобы текущий gameplay interaction path мог реально производить edits в compute mode. Unit proof: `cargo test -p rumpel_world world_block_edit --lib` (`2` tests), `cargo test -p rumpel_player single_chunk_diff --lib` (`2` tests), `cargo test -p rumpel_render compute_ --lib` (`15` tests, включая bridge system и single chunk seed copy). Strict parity proof `.ai_tasks/rumpel_client_compute_20260602_064426.stdout.log`: `matched=2`, `mismatch=0`, `attention=0`, `readback_failed=0`, lifecycle `loaded=2`, `compute_edits=-`, status `0`; measured run caught one known macOS `render_before_render_system` tail spike (`53.02ms`) unrelated to compute parity/readback
- [x] GPU compute queue получил main-world edit application foundation: добавлен `VoxelComputeBlockEdit` message с typed `ChunkPos + LocalBlockPos + BlockId`, `VoxelComputePlugin` регистрирует message и `apply_voxel_compute_block_edits` в `Update`, а source-side application обновляет `VoxelComputeChunkSource.blocks`, соседние `boundary_blocks`, dirty `generation` и `VoxelComputeMeshContract` через единый buffer-based contract builder. `scripts/summarize_profile_log.sh` теперь печатает `compute_edits=...` из `voxel compute block edits applied`. Unit proof `cargo test -p rumpel_render compute_ --lib`: `12` tests, включая source generation/contract update, neighbor boundary contract update и out-of-range edit ignore. Strict parity proof `.ai_tasks/rumpel_client_compute_20260602_063200.stdout.log`: `matched=2`, `mismatch=0`, `attention=0`, `readback_failed=0`, lifecycle `loaded=2`, edit summaries `-`, status `0`; measured run caught one known macOS `render_before_render_system` tail spike (`46.07ms`) unrelated to compute parity/readback
- [x] GPU compute queue получил eviction foundation для исчезающих compute meshes: lifecycle `retain_known_meshes` теперь возвращает evicted entry count, `VoxelComputeBuffers` чистит chunk/boundary/texture input buffers для vanished mesh IDs и сбрасывает stale counter readback state до diagnostic busy gate. `voxel compute lifecycle summary` и `scripts/summarize_profile_log.sh` добавили `evicted_lifecycle_sum`, `evicted_buffers_sum`, `cancelled_readbacks_sum`. Unit proof: `cargo test -p rumpel_render compute_ --lib` теперь включает `compute_lifecycle_evicts_vanished_mesh_entries` и `compute_buffer_retention_tracks_unique_evicted_meshes`. Strict parity proof `.ai_tasks/rumpel_client_compute_20260602_062202.stdout.log`: `matched=2`, `mismatch=0`, `attention=0`, `readback_failed=0`, `loaded=2`, `cancelled_readbacks_sum=0`, measured `6.0s`, `avg_raw_fps=120.0`, worst `10.09ms`, `frames_ge_16ms=0`, status `0`. Stress proof `.ai_tasks/rumpel_client_compute_20260602_062123.stdout.log`: `queue_chunks=26`, `prepared=26`, `loaded=26`, `evicted_*_sum=0`, `cancelled_readbacks_sum=0`; it caught one known macOS `render_before_render_system` tail spike (`39.88ms`) unrelated to compute mismatch/readback
- [x] GPU compute queue получил dirty-generation invalidation foundation: `VoxelComputeChunkSource` хранит monotonically advancing `generation`, `VoxelComputeLifecycle` привязывает `Pending/Building/Loaded` к generation и сбрасывает stale loaded/building entry обратно в `Pending`, если extracted source изменился; stale completion старого generation больше не может закрыть новый rebuild как loaded. Summary parser добавил `invalidated_sum` и `rebuilds_sum`. Unit proof: `cargo test -p rumpel_render compute_ --lib` теперь включает lifecycle requeue, stale completion и source dirty generation tests. GUI stress proof `.ai_tasks/rumpel_client_compute_20260602_061358.stdout.log`: `compute_lifecycle=summaries=9 pending=0 building=0 loaded=26 total=26 queued_sum=26 submitted_sum=26 invalidated_sum=0 rebuilds_sum=0 max_building=8`, `mismatch=0`, `attention=0`, measured `6.0s`, `avg_raw_fps=120.0`, worst `10.10ms`, `frames_ge_16ms=0`, status `0`
- [x] GPU compute queue получил явный lifecycle foundation вместо скрытого render-world `Local<HashSet<AssetId<Mesh>>>`: `VoxelComputeLifecycle` хранит состояния `Pending -> Building -> Loaded`, чистит vanished mesh assets, не допускает повторной постановки loaded chunks и пишет `voxel compute lifecycle summary` с pending/building/loaded totals. `scripts/summarize_profile_log.sh` теперь печатает `compute_lifecycle=...`. Stress proof `.ai_tasks/rumpel_client_compute_20260602_060433.stdout.log`: `compute_lifecycle=summaries=9 pending=0 building=0 loaded=26 total=26 queued_sum=26 submitted_sum=26 max_building=8`, `compute_parity=queue_chunks=26 prepared=26 readback_results=0 mismatch=0 attention=0`, measured `6.0s`, `avg_raw_fps=120.0`, worst `9.96ms`, `frames_ge_16ms=0`, status `0`
- [x] GPU compute queue stress отделен от parity proof: добавлен `just profile-gpu-compute-stress` для production-like queue pressure с default `RUMPEL_GPU_COMPUTE_QUEUE_RADIUS=2`, `RUMPEL_GPU_COMPUTE_MAX_JOBS_PER_FRAME=8`, `RUMPEL_GPU_COUNTERS=0`, `RUMPEL_PROFILE_SECONDS=14`, `RUMPEL_PROFILE_WARMUP_SECONDS=8`. `scripts/summarize_profile_log.sh` теперь добавляет в `compute_parity` строку queue load totals (`prepared_solid_blocks`, `prepared_vertices`, `prepared_indices`, `readback_results`), чтобы stress без counters не выглядел как failed parity. Два 4s-warmup controls `.ai_tasks/rumpel_client_compute_20260602_055554.stdout.log` и `.ai_tasks/rumpel_client_compute_20260602_055632.stdout.log` оба подготовили `26` chunks без ошибок, но поймали поздний `render_before_render_system` spike `43-49ms`; поэтому stress recipe использует 8s warmup. Proof `.ai_tasks/rumpel_client_compute_20260602_055734.stdout.log`: `queue_chunks=26`, `prepared=26`, `readback_results=0`, `mismatch=0`, `attention=0`, `prepared_vertices=230624`, `prepared_indices=345936`, `ready_status=ready`, measured `6.0s`, `avg_raw_fps=119.8`, worst `20.94ms`, `frames_ge_25ms=0`, status `0`
- [x] GPU-driven compute parity slice получил GUI release proof через `just profile-gpu-compute`: recipe теперь запускает `RUMPEL_RENDER_MODE=compute` через macOS GUI launcher с `RUMPEL_GPU_COUNTERS=1`, `RUMPEL_GPU_COMPUTE_QUEUE_RADIUS=0`, `RUMPEL_GPU_COMPUTE_MAX_JOBS_PER_FRAME=1`, `RUMPEL_PRESENT_MODE=immediate`, `RUMPEL_FRAME_LATENCY=1` и readiness gate. `scripts/summarize_profile_log.sh` теперь печатает `compute_parity=...` из stderr, а launchers прокидывают compute queue env. Proof `.ai_tasks/rumpel_client_compute_20260602_054951.stdout.log`: `queue_chunks=2`, `prepared=2`, `matched=2`, `mismatch=0`, `attention=0`, `readback_failed=0`, `ready_status=ready`, measured `6.0s`, `avg_raw_fps=120.0`, worst `10.00ms`, `frames_ge_16ms=0`, status `0`. Radius/job stress для больших регионов держать отдельной задачей, потому что diagnostic counter readback сейчас single-flight
- [x] Pacing matrix получила явный measured-window override: `RUMPEL_PACING_MEASURED_SECONDS` теперь считает client-side `RUMPEL_PROFILE_SECONDS` как `warmup + measured`, сохраняя старое `RUMPEL_PACING_PROFILE_SECONDS` поведение как total-duration fallback. Proof `.ai_tasks/pacing_matrix_20260602_054034/summary.txt`: при `RUMPEL_PACING_PROFILE_SECONDS=999`, `warmup=2`, `measured=3` скрипт передал `client_profile_seconds=5.000`, а профиль завершился с `meas_s=3.0s`. После этого честный 5x6s default-window baseline `.ai_tasks/pacing_matrix_20260602_054054/rollup.txt` дал `ready=5`, `avg_avg=120.0`, `worst_max=10.35`, `ge16_sum=0`, `ge25_sum=0`; low-res 800x450 `.ai_tasks/pacing_matrix_20260602_054212/rollup.txt` также чистый (`worst_max=10.39`, `ge16_sum=0`, `ge25_sum=0`). Текущий packed default стабилен в этих сериях; старые короткие 2s matrix-прогоны не использовать как финальную FPS-истину
- [x] Pacing matrix получила rollup для repeated A/B профилей: `scripts/profile_pacing_matrix.sh` теперь пишет `$matrix_dir/rollup.txt` и печатает его после raw `summary.txt`, группируя строки по `shadow/debug_hud/present/latency/fallback`. Rollup показывает `rows`, `ok`, `non_ok`, `ready`, `timeout`, `avg_min`, `avg_avg`, `worst_max`, суммарные `ge16/ge25/slow` и max по frame/render/window/packed фазам. Проверка prepare-only `.ai_tasks/pacing_matrix_20260602_053415/rollup.txt` корректно дала `ok=0 non_ok=1`, а короткий proof `.ai_tasks/pacing_matrix_20260602_053428/rollup.txt` для `immediate/1` с 2 повторами дал `ready=2`, `avg_avg=120.1`, `worst_max=10.02`, `ge25_sum=0`
- [x] HUD/UI overlay получил диагностический выключатель без поломки FPS-профиля: `RUMPEL_DEBUG_HUD=0` отключает HUD systems и one-shot camera debug logging, но `FrameTimeDiagnosticsPlugin` остается включенным для `rumpel_client` profiling. Launchers и `scripts/profile_pacing_matrix.sh` прокидывают env, matrix получил `RUMPEL_PACING_DEBUG_HUD_VALUES`, а `just profile-packed-no-hud` запускает packed proof без overlay. A/B `.ai_tasks/pacing_matrix_20260602_052748/summary.txt` при новом packed default `shadow=0`: `debug_hud=1` 3/3 clean (`avg_fps≈120`, worst `10.21/10.35/15.66ms`, `ge16=0`, `ge25=0`), `debug_hud=0` 3/3 clean (`worst=10.01-10.25ms`, `ge16=0`, `ge25=0`). HUD не является текущим bottleneck; оставлен включенным по умолчанию, выключатель нужен для диагностики UI/render overlay cost
- [x] Packed GUI profile убрал неиспользуемый Bevy directional shadow path по умолчанию: `RUMPEL_RENDER_MODE=packed` теперь default `shadows=0`, surface/compute сохраняют default `shadows=1`, а `RUMPEL_SHADOWS=1/0` остается явным override. `scripts/profile_pacing_matrix.sh` получил `RUMPEL_PACING_SHADOW_VALUES` и `RUMPEL_PACING_REPEATS` для повторяемых A/B редких хвостов. Proof matrix `.ai_tasks/pacing_matrix_20260602_051844/summary.txt`: при `shadows=1` 1 из 3 повторов поймал `worst=26.11ms`, `ge25=1`, `manage_views_us=19953`, `prepare_windows_us=16009`, packed `render_us=26`; при `shadows=0` 3/3 повтора остались clean (`worst=10.01-10.38ms`, `ge16=0`, `ge25=0`). Новый packed default proof `.ai_tasks/pacing_matrix_20260602_052157/summary.txt`: `shadow=0`, `avg_fps=120.0`, worst `10.09ms`, `ge16=0`, `ge25=0`, packed `render_us=15`
- [x] Window-size diagnostic для GUI packed pacing: клиент принимает `RUMPEL_WINDOW_WIDTH`/`RUMPEL_WINDOW_HEIGHT`, profile start/summary пишет `window_size`, terminal/app launchers и `scripts/profile_pacing_matrix.sh` прокидывают размер, а `just profile-packed-low-res` запускает `800x450` diagnostic без изменения render distance. Moving baseline default `.ai_tasks/pacing_matrix_20260602_051006/summary.txt`: `avg_fps=119.3`, worst `28.05ms`, `ge25=1`, packed `render_us=30`; moving `800x450` `.ai_tasks/pacing_matrix_20260602_051027/summary.txt`: `avg_fps=120.0`, worst `10.45ms`, `ge16=0`, `ge25=0`, packed `render_us=30`. Stationary `800x450` `.ai_tasks/pacing_matrix_20260602_050922/summary.txt` также дал `avg_fps=120.0`, worst `10.82ms`, `ge16=0`. Основной `profile-packed` остается на default window size; low-res используется как диагностический proof, что оставшиеся хвосты baseline находятся в window/pixel/render-schedule path, не в packed draw/mesh/upload
- [x] GUI profiling readiness gate стал двухступенчатым для autopilot: после stationary-ready он выпускает автопилот, ждёт motion pre-roll (`RUMPEL_PROFILE_AUTOPILOT_PREROLL_SECONDS`, default `2.0s`) и только потом стартует measured window. Motion pre-roll проверяет frame stability, но не требует нулевых packed uploads/builds, потому что полет сам провоцирует streaming. Proof `just profile-packed` `.ai_tasks/rumpel_client_packed_20260602_045645.stdout.log`: `present_mode=immediate frame_latency=1`, `autopilot_preroll=2.0s`, `ready_status=ready`, measured `6.0s`, `avg_raw_fps=85.8`, worst `25.57ms`, `frames_ge_25ms=2`, packed worst `render_us=21`. Контроль `RUMPEL_FRAME_LATENCY=default` `.ai_tasks/rumpel_client_packed_20260602_045400.stdout.log` остался нестабильным (`ready_status=timeout`, `avg_raw_fps=44.2`, `frames_ge_25ms=137`, worst `46.67ms` в `prepare_windows`), поэтому baseline возвращен на `immediate/1`, а `profile-packed-default-latency` оставлен diagnostic
- [x] Pacing matrix summary стал ready-aware: `scripts/profile_pacing_matrix.sh` теперь показывает `ready`, `ready_t`, `meas_s` рядом с FPS/worst columns и принимает `RUMPEL_PACING_READY_STABLE_FRAMES`, `RUMPEL_PACING_READY_FRAME_MS`, `RUMPEL_PACING_READY_MAX_EXTRA_SECONDS`. Короткий one-row proof показал `ready=timeout`, `ready_t=5.00s`, `meas_s=3.0s`, то есть таблица теперь явно подсвечивает не-clean прогон
- [x] GUI profile measured window получил readiness gate против холодных rebuild/window spikes: `RUMPEL_PROFILE_READY_GATE=1` в GUI `just` recipes и pacing matrix держит autopilot до readiness, затем ждёт после warmup 30 стабильных кадров мягче `25ms` без packed pending/build/upload/compaction активности, пишет `profile ready ...`, сохраняет целевую measured duration после готовности и тайм-аутится через `RUMPEL_PROFILE_READY_MAX_EXTRA_SECONDS` вместо зависания. Proof `.ai_tasks/rumpel_client_packed_20260602_043609.stdout.log`: `ready status=ready t=7.22s`, measured `6.0s`, `avg_raw_fps=120.0`, worst `10.65ms`, `frames_ge_16ms=0`
- [x] GUI pacing tooling теперь явно показывает недоступные present modes: `scripts/summarize_profile_log.sh` извлекает `PresentMode ... Falling back ...` из stderr и печатает `present_mode_fallback`, а `scripts/profile_pacing_matrix.sh` добавляет колонку `fallback` и держит `bevy_render::view::window=info` в дефолтном `RUMPEL_PACING_RUST_LOG`, чтобы будущие Mailbox/Fifo проверки не выглядели как реальные режимы, если Bevy/Metal их заменил
- [x] Superseded later same day by the two-stage readiness proof above. GUI packed pacing baseline временно переводился с жесткого `immediate/1` на `immediate/default` latency budget: новая wall-clock матрица `.ai_tasks/pacing_matrix_20260602_040644/summary.txt` показала, что `immediate/1` при схожем throughput дает worst `20.99ms`, `frames_ge_16ms=58`, dominated by `prepare_windows`, а warm proof `.ai_tasks/rumpel_client_packed_20260602_041405.stdout.log` с `immediate/default` подтвердил `avg_raw_fps=120.0`, worst `12.23ms`, `frames_ge_16ms=0`, `worst_render_prepare_windows_us=4321`. Subsequent readiness-gated controls showed `immediate/default` can lock into `30/60` window pacing, so it is no longer the baseline
- [x] Headless packed benchmark выровнен по wall-clock phase timers и warmup: `frame_ms` теперь берется из `frame_wall_us` (а Bevy `Time::delta` пишется отдельно как `bevy_delta_ms`), чтобы slow/worst snapshots совпадали с render-phase breakdown; `scripts/profile_packed_headless.sh` и wait-matrix теперь по умолчанию используют `RUMPEL_PROFILE_WARMUP_SECONDS=4`, как GUI recipes. Proof `.ai_tasks/rumpel_client_packed_headless_20260602_040137.stdout.log` с `warmup=4.0s` дал `avg_raw_fps=540.6`, worst `12.86ms`, `frames_ge_16ms=0`, а default proof `.ai_tasks/rumpel_client_packed_headless_20260602_040202.stdout.log` подтвердил `warmup=4.0s`, `avg_raw_fps=435.3`, worst `9.90ms`, `frames_ge_16ms=0`; 3s warmup ранее ловил поздний macOS PipelineCache compile (`render_before_render_system_us=21384`) как runtime spike
- [x] Bevy render tail split до уровня `RenderSystems::Render`/`Prepare`: profile logs и summary теперь пишут `render_before_render_system_us`, `render_system_us`, `render_system_tail_us`, а также `render_prepare_resources_us`, `render_prepare_resources_collect_us`, `render_prepare_resources_flush_us`, `render_prepare_bind_groups_us`, `render_prepare_after_bind_groups_us`; proof с коротким warmup `.ai_tasks/rumpel_client_packed_headless_20260602_034737.stdout.log` поймал ранний macOS sync pipeline compile (`worst_frame_ms=23.89`, `worst_render_before_render_system_us=20266`, `worst_render_system_us=2880`), а default warmup `.ai_tasks/rumpel_client_packed_headless_20260602_034855.stdout.log` дал `avg_raw_fps=546.9`, worst `16.98ms`, `frames_ge_25ms=0`, где остаточный хвост уже `worst_render_prepare_resources_us=11164`; обновленная wait matrix `.ai_tasks/headless_wait_matrix_20260602_035019/summary.txt` показала, что `0ms` остается лучшим throughput baseline (`avg_raw_fps=577.3`, worst `10.53ms`), а `1/2ms` wait не является фиксом
- [x] Full-frame GPU timestamp probe вокруг Bevy `CameraDriver` доведен до рабочего render-pass marker path: `RUMPEL_RENDER_GPU_FRAME_TIMESTAMPS=1` пишет `render_gpu_camera_driver_us`, raw delta/readback counters, а `just profile-packed-headless-all-gpu-timestamps` сверяет его с packed pass GPU timestamps; proof `.ai_tasks/rumpel_client_packed_headless_20260602_033503.stdout.log` дал `avg_raw_fps=425.1`, worst `29.32ms`, `worst_render_gpu_camera_driver_us=1182`, packed `gpu_pass_us=406`, но `worst_render_graph_tail_us=28010`, так что spike остается в Bevy render tail / submit / queue-backpressure зоне, не в packed draw или CameraDriver GPU work
- [x] Проверена headless wait matrix для гипотезы GPU queue backpressure: `.ai_tasks/headless_wait_matrix_20260602_030419/summary.txt` показал `0ms avg_raw_fps=457.4 worst=38.66ms graph_tail=37037us`, `1ms avg_raw_fps=462.5 worst=41.55ms graph_tail=39862us`, `2ms avg_raw_fps=416.8 worst=26.41ms graph_tail=24550us`; wait частично снижает худший render tail, но не устраняет spike и режет throughput, так что это diagnostic knob, а не packed renderer fix
- [x] Headless render spike локализован новым top-level CameraDriver probe: `.ai_tasks/rumpel_client_packed_headless_20260602_030024.stdout.log` показал `worst_frame_ms=39.71`, но `worst_render_camera_driver_us=597`, `worst_render_core3d_us=584`, packed `render_us=41`; почти весь хвост был `worst_render_graph_tail_us=37779`, то есть вне camera graph/packed pass, вероятнее command submit / GPU queue backpressure zero-wait headless loop
- [x] Добавлен opt-in headless renderer-only profile для Bevy `0.18`: `RUMPEL_HEADLESS_RENDER=1` отключает primary window/Winit runner, пишет камеру в `RenderTarget::Image`, а `just profile-packed-headless` запускает packed benchmark напрямую; короткий release-прогон `.ai_tasks/rumpel_client_packed_headless_20260602_025315.stdout.log` дал `avg_raw_fps=472.3`, `render_target=headless`, `worst_render_prepare_windows_us=72`, packed worst `render_us=35`
- [x] Проверен throwaway-порт на Bevy `0.19.0-rc.2` / `wgpu 29.0.3`: `cargo check -p rumpel_client` и warm `just profile-packed` в `/tmp/rumpelrust_bevy019_exp` прошли, но clean profile остался тем же классом swapchain acquire (`avg_raw_fps=124.8`, `worst_frame_ms=21.37`, `worst_render_prepare_windows_us=19460`, packed `render_us=10`), поэтому wgpu 29 не является FPS-фиксом
- [x] Render schedule phase profiling добавил разбивку Bevy `RenderSystems` и отдельный `prepare_windows` probe; контрольный `just profile-packed` показал, что worst frame `22.54ms` почти целиком сидит в swapchain acquire (`worst_render_prepare_windows_us=20923`), а packed render остается микроскопическим (`render_us=12`)
- [x] Исторический low-latency frame-pacing baseline был закреплен как `RUMPEL_PRESENT_MODE=immediate` / `RUMPEL_FRAME_LATENCY=1`; контрольный `just profile-packed` дал `avg_raw_fps=122.2`, `worst_frame_ms=21.11`, `frames_ge_25ms=0`. Позже superseded текущим `immediate/default` GUI pacing baseline выше
- [x] Frame profiling получил точный `render_core3d_us` через Bevy render-graph probe-ноды вокруг полного `Core3d` subgraph; profile logs, `scripts/summarize_profile_log.sh`, и `scripts/profile_pacing_matrix.sh` теперь показывают эту колонку рядом с `render_schedule_us`, чтобы отделять actual 3D graph work от present/window scheduling tail
- [x] Frame-pacing experiments получили `RUMPEL_FRAME_LATENCY` для сравнения Bevy `desired_maximum_frame_latency` (`1`, `2`, `3`, `default/none`) в profile logs и `scripts/profile_pacing_matrix.sh` / `just profile-pacing-matrix` для матрицы `present_mode x frame_latency`; macOS app launcher теперь готовит валидный Mach-O `.app` и `/tmp/rumpel_client_app_*.sh` с `open --env --stdout --stderr`, а profile summary явно пишет `profile_log_fallback_from` при fallback на старый лог
- [x] Frame-phase profiling очищен от недостоверной детализации Bevy render subphases: stdout теперь пишет надежные `frame_wall_us`, `frame_main_us`, `frame_tail_us`, `render_schedule_us`; контрольный packed GUI профиль `.ai_tasks/rumpel_client_packed_20260601_212549.stdout.log` дал `avg_raw_fps=187.0`, `worst_frame_ms=21.21`, `frames_ge_25ms=0`, где худший кадр почти целиком `frame_tail_us=20033` / `worst_render_schedule_us=21056`, а packed path оставался легким (`prepare_us=93`, `render_us=10`)
- [x] Dev-показания FPS/packed renderer теперь снимаются из stdout profile logs через `scripts/summarize_profile_log.sh` / `just profile-summary`; скриншоты остаются только визуальной проверкой мира, текстур, камеры и fog
- [x] Packed region compaction стала deferred по умолчанию (`RUMPEL_PACKED_DEFER_COMPACTION=1`): release GUI профиль после warmup дал `avg_raw_fps=196.6`, `worst_frame_ms=26.01` с одним slow frame без renderer/streaming work, а high-camera screenshot `.ai_tasks/rumpel_client_packed_20260601_195409.png` подтвердил видимый текстурированный мир (`FPS 322.3`, `Packed Q 98883/360952`, `Draws: 1`)
- [x] Packed active-region rebuilds переведены на bounded queue (`RUMPEL_PACKED_MAX_REBUILDS_PER_FRAME=2`): контрольный GUI профиль дал `avg_raw_fps=185.0`, `worst_frame_ms=21.40`, `frames_ge_25ms=0`, а worst frame ограничился `stream_rebuild_regions=2` и `uploaded_this_frame=3`
- [x] Packed profiling теперь пишет slow-frame события и histogram buckets (`frames_ge_16ms`, `frames_ge_25ms`, `frames_ge_33ms`), чтобы отличать startup/upload bursts от стабильного runtime pacing
- [x] Packed arena prealloc получил headroom по умолчанию (`RUMPEL_PACKED_ARENA_HEADROOM=2`), а контрольный GUI-прогон подтвердил `arena_capacity_quads=2097152` без runtime compaction и без кадров `>=25ms` после warmup
- [x] macOS GUI screenshot capture поднимает окно `rumpel_client` перед `screencapture`; свежий high-camera screenshot показывает видимый текстурированный мир (`Packed Q 93871/360950`, `Draws: 1`)
- [x] Packed GPU timestamp diagnostic на Apple M4/Metal: render pass занимает примерно `1.3-1.9ms`, поэтому оставшиеся редкие `30ms+` кадры не объясняются packed draw/prepare/upload path
- [x] Packed batches больше не копируют большие `Vec<PackedVoxelQuad>` при каждом extract в render world: quad storage переведен на `Arc<Vec<_>>`, face-range scan кэшируется по generation, а профиль теперь показывает `packed_prepare_us`
- [x] macOS GUI launcher стал устойчивее к LaunchServices lookup сбоям: Terminal AppleScript теперь таргетится по абсолютному пути, а prepared `.app` очищается от xattrs и подписывается ad-hoc после сборки
- [x] Packed profiling теперь читает direct metrics snapshot из bridge и добавляет main-side timings: `packed_stream_us`, `packed_build_task_us`, `packed_compaction_us`, spawned/rebuilt/compacted counts для привязки worst frame к CPU streaming path
- [x] Packed face-range culling включен по умолчанию с порогом `4096` quads: packed-профиль держит один draw submission (`Draws: 1`) и снижает видимые quads примерно до `85k-94k` вместо прежних `112k-127k` без урезания view radius
- [x] Packed profiling расширен worst-frame snapshot: профиль теперь пишет `interval_worst_frame_ms`, `avg_raw_fps`, `worst_frame_ms` и packed-метрики худшего кадра, чтобы следующие FPS-правки связывать с build/upload/compaction/render причиной
- [x] Metal/macOS packed indirect fallback переведен на CPU-visible compact buffer + `multi_draw_indirect`: HUD/профиль теперь показывают `Cmds: 62`, `Vis: 18-21`, `Draws: 1` вместо per-visible draw loop
- [x] Packed GPU cull получил compact/count путь для backend-ов с `MULTI_DRAW_INDIRECT_COUNT`; на Metal/macOS path корректно остается в CPU-visible indirect fallback без регресса в полный command loop
- [x] Phase 4 foundation: opt-in packed GPU frustum culling for indirect draw commands through `RUMPEL_PACKED_GPU_CULL=1`, with GPU output indirect buffer, CPU fallback, profiling metrics, and high-camera screenshot verification
- [x] Phase 2 foundation: bounded compute mesh job queue для `RUMPEL_RENDER_MODE=compute` с несколькими world chunks, per-job CPU mesh contracts, заранее выставленным draw count и opt-in diagnostic readback только через `RUMPEL_GPU_COUNTERS=1`
- [x] Реализована высокопроизводительная отрисовка через GPU-driven indirect draw command buffer и структурированные draw parameters, с поддержкой аппаратного `multi_draw_indirect` (при наличии поддержки) и автоматическим loop-based indirect циклом `draw_indirect` без смены bind groups или pipeline state.
- [x] Реализована единая contiguous `PackedQuadGpuArena` для packed renderer с автоматическим power-of-two ростом, ленивой инициализацией GPU-буфера для предотвращения Bevy startup-паник на старте, и оптимизацией дублирующихся GPU-аплоадов.
- [x] Исправлена фрагментация `PackedQuadGpuArena`: region slots больше не округляются до power-of-two, добавлена in-place compaction активных stable slots и профильные метрики `arena_slot_quads`/`arena_compactions`
- [x] Разделены packed loaded cache и active render set: radius+2 halo остается кэшем, а `PackedQuadBatches`, HUD chunks и draw commands собираются только из строгого view radius
- [x] Добавлена player-relative X/Z сетка стриминга packed quad чанков вокруг камеры для `RUMPEL_RENDER_MODE=packed` с процедурной генерацией 4 соседних чанков на лету для crack-free neighbor-aware culling, кэшированием BindGroup/Uniforms в Prepare stage и real-time метриками
- [x] Добавлен `RUMPEL_RENDER_MODE=packed` для изолированного benchmark packed renderer без двойной отрисовки
- [x] Opt-in PackedVoxelQuad debug producer, pipeline metrics, HUD/profiling integration, и custom vertex pulling renderer skeleton с Render Graph node интеграцией (RUMPEL_PACKED_QUAD_DEBUG=1 и RUMPEL_PACKED_QUAD_RENDERER=1)
- [x] Первый production-ready слой для packed voxel quads / vertex pulling (контракт данных в виде 16-byte Pod ABI, neighbor-aware culling, CPU-билдер с полноценным 2D слиянием, GPU upload layer и тесты)
- [x] Инициализация проекта на Rust + Bevy
- [x] Настройка базового FPS-управления и камеры
- [x] Генерация тестового чанка (плоский мир)
- [x] Создание структуры RAG-памяти
- [x] Регистрация агентов (Геймдиректор, Программист, QA)
- [x] Базовый Lua-моддинг через `mlua` и `register_block`
- [x] Первичная интеграция `bevy_voxel_world` как runtime backend для стриминга и мешинга мира
- [x] Surface-aware streaming чанков поверхности вместо 3D-сферы вокруг игрока
- [x] Async mesh generation для чанков с лимитом GPU upload на кадр
- [x] Автоматический profiling/autopilot run клиента для проверки FPS в движении
- [x] LOD чанков без урезания дальности прорисовки мира
- [x] High-FPS режим: no-vsync, continuous update loop, unlit terrain, MSAA off
- [x] Lua API autocomplete/linting через `.luarc.json` и `api_stub.lua`
- [x] Bounded Lua `world_gen.lua` post-pass для spawn chunk без запуска в startup-модах
- [x] Быстрый macOS linker `ld64.lld` вместе с `sccache`
- [x] Distance LOD и throttled mesh uploads для surface streaming
- [x] Frame-phase profiling для render/upload/streaming, чтобы целиться в стабильные 200+ FPS измеримо
- [x] Backface culling для streamed terrain materials
- [x] Opt-in DepthPrepass/OcclusionCulling flags для measured FPS experiments
- [x] Palette + RLE chunk codec foundation
- [x] VoxelQuadMaterial foundation для texture-array terrain и repeat UV merged quads
- [x] Greedy merge top faces and side walls для surface heightmap regions
- [x] Binary face-mask greedy foundation для surface heightmap regions
- [x] `just profile-client` для повторяемого autopilot FPS профиля
- [x] Creative flight surface guard, чтобы игрок не проваливался под heightmap-мир
- [x] GPU-driven voxel roadmap сохранен как ADR-002 для Codex/Antigravity агентов
- [x] Явный выбор render mode через `RUMPEL_RENDER_MODE=surface|compute`
- [x] Compute prototype parity chunk подключен к `rumpel_world::generate_chunk_with_context`
- [x] Persistent Lua runtime получил `get_height` и безопасные block API fallbacks для world tick модов
