[package]
name = "rumpel_debug"
version = "0.1.0"
edition = "2024"

[features]
default = []
inspector = ["dep:bevy_egui", "dep:bevy-inspector-egui"]

[dependencies]
bevy = { workspace = true }
bevy_egui = { workspace = true, optional = true }
bevy-inspector-egui = { workspace = true, optional = true }
rumpel_prelude = { workspace = true }
rumpel_player = { path = "../rumpel_player" }
rumpel_render = { workspace = true }
