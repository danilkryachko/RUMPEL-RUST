# ADR-002: GPU-driven voxel rendering roadmap

## Status
Accepted

## Context
RUMPEL RUST already has two voxel render paths:

- the default `surface_streaming` path, which is production-facing today: surface-aware heightmap regions, `VoxelQuadMaterial`, texture-array tiles, repeat UVs, LOD, async CPU mesh tasks, upload throttling, and profiling metrics;
- the opt-in GPU compute prototype, which dispatches `mesh_generator.wgsl` and writes a single chunk mesh into Bevy mesh buffers.

The producer wants the long-term engine direction to move toward GPU-driven terrain: compute meshing first, then packed quads, indirect rendering, GPU culling, and later hierarchical far-field representations. The current CPU surface path remains the baseline/old mode for visual and profiling comparison while the GPU path reaches parity.

## Decision
Use a phased GPU-driven migration instead of replacing the renderer in one step.

1. **Render mode boundary:** keep surface streaming as the default renderer. Use `RUMPEL_RENDER_MODE=surface` for the baseline and `RUMPEL_RENDER_MODE=compute` for the GPU prototype. The legacy `RUMPEL_COMPUTE_PROTOTYPE=1` remains accepted as an alias.
2. **Phase 1 - compute parity slice:** feed one real world chunk or small region into the compute mesher, keep the output contract explicit, and measure it against the surface path. This phase must not depend on MDI, SVO, or GPU culling.
3. **Phase 2 - GPU streaming queue:** expand from one chunk to a bounded queue of compute jobs with per-job counters, no hot-path readbacks, deterministic lifecycle rules for loaded/building/pending regions, GPU-owned output buffers, and a direct compute terrain render phase before compacting the ABI.
4. **Phase 3 - packed quads / vertex pulling:** move away from full Bevy vertex attributes when measured upload bandwidth becomes the bottleneck. Store compact quad commands and draw them through a custom material or render phase.
5. **Phase 4 - indirect rendering and GPU culling:** introduce indirect draw buffers only after GPU meshing owns stable buffers and counters. CPU should submit coarse work, while GPU culls and draws visible terrain.
6. **Phase 5 - far-field hierarchy:** evaluate SVO/SVDAG or raymarched far terrain after near-field edit semantics, block registry mapping, and chunk streaming are stable.

## Guardrails
- Do not reduce view radius to hide performance issues unless the producer explicitly asks.
- Do not make an experimental compute slice the default until it has visual parity, profiling data, and a safe comparison path.
- Do not add per-frame GPU readbacks to production terrain; use readbacks only for diagnostics or bounded profiling.
- Preserve data-driven block IDs, texture atlas tile mapping, and Lua worldgen boundaries.
- Keep `surface_streaming` and `VoxelQuadMaterial` intact while the compute path matures.

## Progress Notes
- 2026-06-04: GPU-generated planned-region staging now receives `PackedGpuGenerationBatchSummary::active_chunk_job_count` as the reserve hint, removing another active chunk-range count pass before building prepared region metadata. Verified with focused generated prepare/summary tests and full `just verify`.
- 2026-06-04: GPU-generated render prepare now passes `PackedGpuGenerationBatchSummary::active_chunk_job_count` into generation job collection as the reserve hint, removing the extra active chunk-range scan that existed only to size staging buffers. Verified with focused generated prepare/summary tests and full `just verify`.
- 2026-06-04: CPU visible indirect selection now separates index and command collection through an explicit collection mode. Draw-indirect fallback keeps visible indices/metrics without building draw-command copies, while CPU-visible compact still collects commands for buffer upload. Verified with focused selection tests and full `just verify`.
- 2026-06-04: CPU indirect render path now skips CPU visible selection entirely for full `multi_draw_indirect` frames when CPU-visible compact is disabled; that mode draws the full indirect buffer and retains its metadata-only visible metrics pass. Verified with focused selection tests and full `just verify`.
- 2026-06-04: CPU-visible indirect compact fallback now skips visible draw-index collection when the compact command buffer is available; indices are collected only for the draw-indirect fallback, while commands and visibility metrics stay intact. Verified with focused selection tests and full `just verify`.
- 2026-06-04: GPU-generated cull dispatch reuse now exits before compute pipeline, view uniform, and cull buffer lookups; stable reused frames only compute the source/view dispatch signature and publish cached visible metrics. Verified with focused cull tests and full `just verify`.
- 2026-06-04: GPU-generated rebuild column staging no longer maintains the unused `region_column_base` counter while copying columns into the upload scratch buffer. Verified with focused render checks and full `just verify`.
- 2026-06-04: GPU-generated render prepare now uses the producer-side active chunk job count from `PackedGpuGenerationBatchSummary` instead of scanning every generated chunk range before steady-frame skip/capacity checks. Verified with focused render tests and full `just verify`.
- 2026-06-04: GPU-generated render prepare now uses the producer-side batch summary's total column count instead of scanning every generated batch before the steady-frame early return. Verified with focused render tests and full `just verify`.
- 2026-06-04: GPU-generated render prepare now builds planned regions directly from the reusable allocation-plan scratch instead of cloning the whole allocation `HashMap` during rebuild/region-shift work. Verified with focused render checks and full `just verify`.
- 2026-06-03: GPU-generated region prefetch now uses bounded nearest-region selection before sorting the selected budget, reducing prefetch ordering work when missing-region lists are larger than the per-frame prefetch window. Verified with focused render checks and clean temp `just verify`.
- 2026-06-03: CPU-packed indirect command and draw-param buffers now use byte signatures to skip unchanged `write_buffer` uploads on stable frames, with signatures reset on buffer recreation. Verified with focused render checks and clean temp `just verify`.
- 2026-06-03: CPU-packed indirect prepare now caches the global draw bind group with an arena/params/palette generation key, avoiding per-frame bind-group creation on stable frames while preserving invalidation on buffer recreation. Verified with focused render checks and clean temp `just verify`.
- 2026-06-03: CPU-packed GPU cull prepare now signature-gates metadata/config uploads, mirroring the generated cull path's stable-frame behavior without reusing CPU-path dispatch results. Verified with focused render checks and clean temp `just verify`.
- 2026-06-03: Packed indirect draw-mode selection now stores a static string in render state, removing a small per-frame allocation. Also restored the profiling draw-mode label helper to module scope after clean temp verification exposed it was nested inside `env_flag`. Verified with clean temp `just verify`.
- 2026-06-03: GPU-generated active chunk target hashes now use a deterministic circular view-radius scan helper instead of HashSet iteration, preserving count/hash semantics while avoiding order-dependent cache signatures. Verified on a clean remote+patch temp checkout with render check, focused signature test, render clippy, plus workspace `just verify` in the active worktree.
- 2026-06-03: Packed indirect prepare now records confirmed batch generations only when the allocation key/generation signature changes, preserving dirty-range pruning while avoiding repeated shared HashMap rebuilds on stable frames. Verified with render check, focused signature test, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: GPU-generated active region selection now uses the arithmetic region/view-radius predicate instead of per-region HashSet chunk scans. Added parity coverage against the previous active-chunk set scan and verified render checks, focused tests, clippy, and `just verify`.
- 2026-06-03: Deferred packed region compaction now coalesces post-compaction generation and flag updates into one Arc-backed packed batch mutable access. Static proof: render check, focused deferred compaction test, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: CPU packed `PackedQuadBatches` extraction now shares the batch vector through `Arc<Vec<_>>`; streaming/material mutation paths use explicit COW access. This removes the stable per-frame extracted batch Vec clone on unchanged CPU packed targets. Static proof: render check, extract-sharing test, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: Packed arena/memory region-count estimates now count region rectangles intersecting the circular view radius arithmetically, removing the runtime `HashSet` allocation. Static proof: render check, brute-force parity region-count test, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: Packed texture palette extraction now shares the main-world tile table with `Arc<Vec<_>>` and preserves prepared render-world Vec reuse for GPU uploads. Static proof: render check, focused palette extract-sharing test, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: `PackedGpuGenerationBatches` extraction now shares generated batch storage with `Arc<Vec<_>>`; mutation paths use explicit COW/take helpers. This removes the stable per-frame extracted Vec clone on unchanged GPU-generated targets. Static proof: render check, extract-sharing test, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: Removed the per-update `HashMap` clone from packed dirty-range pruning; confirmed upload generations are read under the existing mutex guard. Static proof: render check, focused dirty-range test, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: Removed the per-frame temporary visible draw-index allocation from GPU-generated cull fallback rendering; compact fallback draws `0..visible_commands`, and non-compact fallback streams visible region draw calls directly. Static proof: render check, renderer tests, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: GPU-generated packed rendering now tracks active chunks inside generated regions, allocates/prepares indirect jobs per active chunk, prefetches loaded-but-inactive regions for sliding windows, carries generated batches across compatible camera shifts, and reuses active chunk-key scratch. Static proof: `cargo check -p rumpel_render --all-targets`, focused active-key scratch test, render clippy with `-D warnings`, and `just verify`.
- 2026-06-03: Packed streaming now builds `WorldGenerationContext` lazily inside the build-spawn loop only when a new task will actually be spawned, after pending/building duplicate checks. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render streaming_grid_radius_targets --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed chunk build task spawning now shares `WorldGenerationContext` through `Arc<WorldGenerationContext>` for all tasks spawned in a frame, avoiding per-task clones of the context's name/id maps. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render streaming_grid_radius_targets --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed CPU region compaction now uses `PackedRegionCompactionScratch` stored on `PackedQuadStreamingState`, reusing `compacted_quads`, `compacted_ranges`, and per-range `chunk_quads` instead of creating a fresh `to_vec()` for every resident range. Static proof: `cargo test -p rumpel_render region_compaction --lib`, `cargo check -p rumpel_render --all-targets`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed material fallback entity sync now reuses `PackedMaterialEntities.active_keys` for active batch retain checks instead of allocating a temporary `HashSet` per sync. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render active_chunk_ranges_emit_only_active_indirect_commands --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: GPU-generated arena free-list best-fit removal now uses `swap_remove` instead of `Vec::remove`; free-list order is not semantic, so slot removal no longer shifts the tail. Static proof: `cargo test -p rumpel_render plan_gpu_generated_arena_allocations --lib`, `cargo check -p rumpel_render --all-targets`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: GPU-generated arena allocation planning now has a non-allocating sorted hot-path API, `plan_gpu_generated_arena_allocations_sorted_into`, backed by reusable `PackedGpuGenerationArenaAllocationPlanScratch`. `PackedGpuGenerationBuffers` stores the output plan and scratch, then swaps the finished plan into `PackedQuadGpuArena.allocations`. Static proof: `cargo test -p rumpel_render plan_gpu_generated_arena_allocations_sorted_matches_unsorted --lib`, `cargo check -p rumpel_render --all-targets`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed region rebuild now reuses `PackedQuadStreamingState.region_rebuild_chunk_keys` instead of collecting a fresh `Vec` for every region rebuild. The scratch is returned on both normal rebuild and empty-region early return. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render rebuild_region_batch_uses_loaded_cache_chunks --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed streaming active chunk membership now reuses `active_render_chunks` and `previous_active_render_chunks` hash sets instead of collecting a new `HashSet` every center-change frame. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render streaming_grid_radius_targets --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed streaming center-change planning now reuses a `PackedQuadStreamingState.render_chunks` vector for the full render target list. The allocating `packed_render_chunks_for_center` wrapper is test-only; production uses `fill_packed_render_chunks_for_center` into reused capacity. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render packed_render_chunks_for_center_matches_radius --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed streaming pending queue now reuses `PackedQuadStreamingState.pending` during center changes instead of creating a local vector before assignment. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render streaming_grid_radius_targets --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed streaming despawn planning now reuses `chunks_to_despawn` and `building_to_despawn` scratch buffers on `PackedQuadStreamingState` instead of allocating local vectors during center-change eviction. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render streaming_grid_radius_targets --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed streaming membership changes now reuse a `PackedQuadStreamingState.membership_changes` scratch vector instead of collecting a temporary `Vec` every center-change frame. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render streaming_grid_radius_targets --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed streaming membership sync no longer creates a local `HashSet` on every center-change frame. `PackedQuadStreamingState` stores a reused `regions_to_sync` scratch set, clears it at frame start, and syncs region active flags from that set. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render streaming_grid_radius_targets --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: GPU-generated render prepare no longer calls `clone()` for planned regions during rebuild. `PreparedPackedGpuGeneratedRegion` is `Copy`, and job/params construction copies it directly from `planned_regions`. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: GPU-generated region cache retain no longer builds a `target_keys` HashSet. The non-skip update path sorts the reused `loaded_region_keys` scratch and uses binary search during `region_cache.entries.retain`, leaving the stable-target early-out free of the extra work. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render generated_region_cache --lib`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: GPU-generated arena allocation planner now has a sorted-input hot path. `prepare_packed_gpu_generated_draw` calls `plan_gpu_generated_arena_allocations_sorted`, avoiding `requests.to_vec()` and a repeated `sort_by_key` for already ordered generated batches. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render plan_gpu_generated_arena_allocations --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed arena allocation prepare now reuses scratch `HashMap`/`Vec` storage in `PreparedPackedQuadIndirectDraw`. Stable and compacted allocation plans are written in-place, and the accepted plan is swapped into `arena.allocations`, leaving the previous map as next-frame scratch. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_plan_stable_arena_allocations --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed arena reallocation no longer recreates `dirty_batch_keys`. Prepare clear/reserve/extend reuses the Vec when reallocation marks all extracted batches full-dirty. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_arena_growth_from_zero --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo test -p rumpel_render test_prune_dirty_ranges_keeps_unconfirmed_generations --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed prepare now reuses one sorted batch order for allocation planning and indirect build. `sort_packed_batch_order` fills `PreparedPackedQuadIndirectDraw::batch_order`, and `plan_stable_arena_allocations_for_order` removes the extra `collect::<Vec<_>>()`/sort from the hot prepare path. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_stable_offsets_for_sorted_input --lib`, `cargo test -p rumpel_render test_plan_stable_arena_allocations_reuses_offsets --lib`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed dirty upload decisions no longer allocate a local `HashSet`. `PreparedPackedQuadIndirectDraw::dirty_batch_key_set` is clear/reserve/fill reused for full-dirty lookup before upload. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_prune_dirty_ranges_keeps_unconfirmed_generations --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed prepared-batch eviction no longer scans extracted batches for every prepared batch. `PreparedPackedQuadIndirectDraw::active_batch_keys` is reused as a HashSet; prepare clear/reserve/fill builds active keys in one pass and `retain` uses O(1) lookup. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_evict_last_chunk_removes_empty_region_batch --lib`, `cargo test -p rumpel_render test_active_chunk_ranges_emit_only_active_indirect_commands --lib`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-03: Packed dirty-range upload no longer clones previous arena allocations. Prepare reads previous allocation state directly from `arena.allocations` until the final move of the new allocation map. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_prune_dirty_ranges_keeps_unconfirmed_generations --lib`, `cargo test -p rumpel_render test_plan_stable_arena_allocations_reuses_offsets --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Packed arena allocation update no longer clones the full allocation HashMap. `new_allocations` remains the lookup source until prepare is done and then moves into `arena.allocations` after the last use. Static proof: `cargo test -p rumpel_render test_plan_stable_arena_allocations_reuses_offsets --lib`, `cargo test -p rumpel_render test_arena_compaction_replans_freed_slots_before_growth --lib`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Packed indirect prepare now reuses staging buffers. `PreparedPackedQuadIndirectDraw` owns `batch_order`, `commands`, `command_metadata`, and `params_staging`; prepare clear/reserve/fill builds the sorted indirect stream without local staging Vec allocation and without replacing the whole resource at frame end. Static proof: `cargo check -p rumpel_render --all-targets`, `cargo test -p rumpel_render test_indirect_buffer_packing --lib`, `cargo test -p rumpel_render test_active_chunk_ranges_emit_only_active_indirect_commands --lib`, `cargo test -p rumpel_render test_collect_visible_indirect_commands_preserves_draw_command_order --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Packed CPU-source GPU cull metadata upload now reuses `PreparedPackedQuadGpuCull::metadata_scratch`. The prepare path no longer builds a temporary `collect::<Vec<_>>()` before `write_buffer`. Static proof: `cargo test -p rumpel_render test_gpu_cull_metadata_from_indirect_command --lib`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Packed indirect visibility accounting no longer allocates `HashSet`s. The renderer counts visible batches by transitions in the grouped `batch_key` stream for CPU-visible selection, estimate path, and multi-indirect metrics path; the unit test now covers duplicate face-range commands from one batch. Static proof: `cargo test -p rumpel_render test_collect_visible_indirect_commands_preserves_draw_command_order --lib`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo test -p rumpel_render packed_gpu_generation_visibility_estimate --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Generated GPU cull metadata upload now reuses persistent scratch. `PreparedPackedQuadGpuCull` owns `metadata_scratch`, and prepare clear/reserve/fill writes metadata without a temporary `collect::<Vec<_>>()` when the cull metadata signature rebuilds. Static proof: `cargo test -p rumpel_render packed_gpu_generation_cull --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Generated indirect CPU fallback visibility now streams visible regions directly. The render path no longer allocates `visible_regions.collect::<Vec<_>>()`; it counts visible regions/quads and emits `draw_indirect` in one pass over prepared regions. Static proof: `cargo test -p rumpel_render packed_gpu_generation_visibility_estimate --lib`, `cargo test -p rumpel_render packed_gpu_generation_cull --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Generated render prepare now reuses a scratch Vec for planned regions. `PackedGpuGenerationBuffers` owns `planned_regions`, prepare clear/reserve fills it repeatedly, and `prepared.regions.clone_from(...)` preserves prepared-side allocation without allocating a local `Vec::with_capacity` on every non-reused prepare. Static proof: `cargo test -p rumpel_render packed_gpu_generation_prepared --lib`, `cargo test -p rumpel_render packed_gpu_generation_visibility_estimate --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Generated region builder no longer post-processes appended chunk columns to apply local region offsets. `append_surface_gpu_generation_columns_for_chunk_with_local_offset` writes the offset during packing, covered by a parity test that checks offset coordinates without changing heights/materials. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render generated_region_cache --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Generated region update now snapshots `edit_store_generation` once per frame. Target matching, cache entries, and invalidation logs use the same generation snapshot instead of repeated `edit_store.generation()` calls. Static proof: `cargo test -p rumpel_render generated_region_cache --lib`, `cargo test -p rumpel_render gpu_generation_contract --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Generated region miss loop no longer recomputes `source_chunk_count` and `expected_column_count` per region. Frame-constant capacity math is hoisted before the active-region loop. Static proof: `cargo test -p rumpel_render generated_region_cache --lib`, `cargo test -p rumpel_render packed_gpu_generation_region_radius_for_view --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Batched neighbor-height sampling now has a no-edits specialization. The baseline generated path returns four deterministic noise heights directly and skips the shared edit-aware sampler branch for each neighbor. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: `SurfaceColumnSource` now caches chunk world origin (`world_origin_x/z`), so traversal and batched neighbor sampling no longer multiply `chunk_pos * CHUNK_SIZE` for every column. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: `surface_neighbor_heights` now accepts `SurfaceColumnSource` directly, so the generated append call-site no longer rebuilds context/edit/perlin/has_edits argument lists for batched neighbor sampling. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated column append now packs four neighbor heights through `surface_neighbor_heights`, computing base world X/Z once per column. The single-height API reuses the same low-level sampler for wall and test callers. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Surface column traversal now uses a private `SurfaceColumnSource` for chunk/context/cell/material/edit/perlin/has_edits state. This keeps traversal signatures compact, computes `has_edits` once per chunk, and prepares the path for cached neighbor-height sampling. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Added an edit-aware parity test for generated GPU surface columns. The test raises a deterministic surface cell through `WorldBlockEdit`, computes the expected `terrain_surface_cell_sample_with_edits`, and verifies GPU column packing stores the same height and top material. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Generated surface neighbor-height sampling now has a baseline fast path. When `WorldEditStore` is empty, CPU and GPU column builders skip neighbor `ChunkPos` creation and per-column `chunk_revision` lookups, falling directly to deterministic noise height; edit-aware sampling remains enabled through an explicit `has_edits` flag. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: Surface and GPU-generated column builders now reuse a single `Perlin` context per chunk. CPU surface generation shares the context between column sampling and wall neighbors, and GPU append shares it between surface samples and neighbor-height packing. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated column append path no longer builds an intermediate `SurfacePackedColumn` Vec. A shared `for_each_surface_packed_column_for_chunk` traversal walks the cell grid once; the CPU builder remains a wrapper, while GPU append converts each surface column directly into `PackedGpuSurfaceColumn`. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render surface_packed_quads --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated cache-miss builder no longer creates a per-source-chunk GPU column Vec. `append_surface_gpu_generation_columns_for_chunk` appends directly into the preallocated region Vec, the existing `build_*` function remains as a wrapper, and the region builder adjusts offsets only on the newly appended slice. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated cache-miss path now preallocates region column storage. New generated cache entries create the region `columns` Vec with `source_chunk_count * packed_gpu_generation_columns_per_chunk(cell_size)` capacity, avoiding growth reallocations while preserving the shared terrain source contract. Static proof: `cargo test -p rumpel_render generated_surface_columns --lib`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated main-world update now reuses a builder Vec for generated batches. `PackedGpuGenerationRegionScratch::generated_batches` collects and sorts batches, then swaps with `gpu_batches.batches` when the signature changes; the scratch Vec is cleared before exit so old `Arc` column storage is not retained beyond the update. Static proof: `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo test -p rumpel_render packed_gpu_generation_batch_summary --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated main-world update now uses reusable region scratch. Loaded region keys, active region triples, and the target-key `HashSet` live in `PackedGpuGenerationRegionScratch` and reuse capacity across frames, while the stable-target early-out still returns before building the target-key set. Static proof: `cargo test -p rumpel_render packed_gpu_generation_target --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated render prepare now reuses CPU scratch buffers for rebuild upload payload. `PackedGpuGenerationBuffers` keeps allocation requests, generation jobs, source columns, and draw params between frames, so rebuilds no longer allocate fresh staging `Vec`s for every generated upload. Static proof: `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo test -p rumpel_render packed_gpu_generation_batch_summary --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated render prepare no longer reconstructs a temporary allocation `HashMap` from prepared regions on rebuild. The arena planner now reads the existing `PackedQuadGpuArena::allocations` map directly, then the arena is atomically replaced with the new plan. This preserves free-slot reuse semantics while removing avoidable per-rebuild collection. Static proof: `cargo test -p rumpel_render plan_gpu_generated_arena_allocations --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated render prepare now consumes a producer-side batch summary. The generated update stores total columns, max quads, max columns, source chunks, and invalid-batch count next to `batch_signature`; render prepare no longer scans batches for validation/totals before the steady skip, and changed detection compares the full signature instead of a key/generation scan. Static proof: `cargo test -p rumpel_render packed_gpu_generation_batch_summary --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated render prepare now uses an O(1) steady batch signature match. The main-world producer stores `batch_signature` for the sorted generated batch set, and render prepare compares signature/count/arena generation instead of scanning every prepared region in `matches_batches`. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated render prepare no longer allocates and sorts a per-frame `Vec<&Batch>`. The main-world producer sorts generated batches by key when the batch set is rebuilt, and render prepare consumes the extracted ordered slice directly on steady frames. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated cull visible estimation now uses a direct prepared-region pass. The generated path no longer builds a metadata `Vec` or uses the generic visible-batch `HashSet` during dispatch estimation; it counts visible commands/quads directly from prepared region bounds and max output quads. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_visibility_estimate --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated cull dispatch path now caches its bind group in prepare. `PreparedPackedQuadGpuCull` stores the generated cull bind group and recreates it only when buffers or source signature change, while the render node uses the cached group and keeps CPU-prepared fallback creation. This removes per-dispatch bind-group creation from the generated cull hot path. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated cull skip path no longer allocates the CPU metadata `Vec`. `PackedQuadGpuCullSource::Generated` now borrows prepared regions, and generated cull metadata for CPU-side visible estimates is collected only after dispatch reuse has been checked. This removes avoidable allocation on steady `generated_cull_dispatch_skipped=true` frames. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated cull node now reuses the previous culled indirect output for identical steady-view dispatch signatures. The signature includes generated source state, cull metadata/config signatures, cull mode, and the view transform; generated cull dispatch is skipped only when those inputs are unchanged. `profile sample`, `profile worst_packed`, summaries, and generated compare reports expose `generated_cull_dispatch_skipped`. Static proof: `cargo fmt --all`, `bash -n` for compare/profile scripts, synthetic summary check, `cargo test -p rumpel_render packed_gpu_generation_cull --lib`, `cargo test -p rumpel_render packed_gpu_cull_dispatch_signature --lib`, `cargo clippy -p rumpel_render --all-targets --all-features -- -D warnings`, and `just verify`.
- 2026-06-02: GPU-generated render/cull cache telemetry is now exposed in the profile contract. `profile sample`, `profile worst_packed`, summaries, and generated compare reports print `generated_prepare_skipped`, `generated_cull_metadata_uploaded`, and `generated_cull_config_uploaded` so A/B logs can prove steady render-prepare reuse and skipped cull metadata/config uploads. Static proof: `cargo fmt --all`, `bash -n` for compare/profile scripts, synthetic summary check, `cargo test -p rumpel_render packed_gpu_generation_cull_signatures --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, and `just verify`.
- 2026-06-02: GPU-generated cull metadata signature is now stored in `PreparedPackedGpuGeneratedDraw`. The steady render-prepare skip path no longer re-hashes bounds/max-quads for every generated region and passes the cached signature directly into generated cull preparation. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_cull_signatures --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, and `just verify`.
- 2026-06-02: GPU-generated cull prepare now avoids rewriting static metadata/config buffers on steady frames. `PreparedPackedQuadGpuCull` stores metadata/config signatures, and the generated cull path only rebuilds metadata/config uploads when region bounds, max quads, command count, compact mode, or buffer allocation changes. This removes avoidable metadata `Vec` construction and `write_buffer` work from the steady render-prepare skip path. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_cull_signatures --lib`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, and `just verify`.
- 2026-06-02: GPU-generated render prepare now has an early skip for already-prepared batches. `PreparedPackedGpuGeneratedDraw::matches_batches` lets the steady path bypass allocation planning, planned-region rebuild, buffer writes, and bind-group rebuild when prepared regions, arena generation, and extracted batches already match, while still refreshing per-view GPU cull preparation. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_prepared_matches_batches --lib`, and `just verify`.
- 2026-06-02: GPU-generated arena allocation planning now avoids active-key `HashSet` construction and full `existing_allocations` cloning. `plan_gpu_generated_arena_allocations` uses sorted request keys, binary search for active existing slots, and copies only the allocations that can be reused while preserving evicted free-slot reuse. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render plan_gpu_generated_arena_allocations --lib`, and `just verify`.
- 2026-06-02: GPU-generated skipped update path no longer allocates/hashes a loaded-region `HashSet`. The update first collects loaded keys in a compact `Vec` for active-signature work, then only materializes the `HashSet` when early-out fails and cache retain is actually needed. This trims steady and unchanged-active moving-frame CPU overhead. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, and `just verify`.
- 2026-06-02: GPU-generated active-region checks now use an O(1) region AABB vs circular view-radius test instead of scanning every chunk in the region. This reduces the per-frame CPU cost of the active-region signature path, especially as region size grows. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render region_has_active_chunks --lib`, and `just verify`.
- 2026-06-02: GPU-generated moving-camera early-out now keys off active-region signature instead of exact camera chunk equality. The main-world update collects loaded/active region keys, records `active_region_hash`, and skips cache walk/source rebuild when the generated window, source contract, edit-store generation, and active region set are unchanged even if the camera moved to another chunk. This targets smooth-flight CPU spikes without underloading newly active generated regions. Static proof: `cargo fmt --all`, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, and `just verify`.
- 2026-06-02: GPU-generated region cache lifecycle is now visible in packed profiling. `PackedQuadPipelineStats`, `profile sample`, `profile worst_packed`, summaries, and generated visual/headless compare reports expose `generated_cache_hits`, `generated_cache_misses`, `generated_cache_invalidated`, and `generated_cache_evicted`. This separates stable-target skips from moving-camera cache reuse/rebuild/eviction costs before the next lifecycle optimization pass. Static proof: `cargo fmt --all`, `bash -n` for compare/profile scripts, synthetic summary check, `cargo test -p rumpel_render packed_gpu_generation_target --lib`, and `just verify`.
- 2026-06-02: GPU-generated stable-target early-out now has explicit telemetry. Packed stats expose `generated_update_us` and `generated_update_skipped`; `profile sample`, `profile worst_packed`, summaries, and visual/headless generated compare reports print both fields. This makes the early-out measurable: stable-camera runs should show skipped planning and the remaining CPU cost of the generated update system. Static proof: `cargo fmt --check`, `bash -n` for compare/profile scripts, synthetic summary check, and `just verify`.
- 2026-06-02: GPU-generated region update now has a stable-target early-out. `PackedGpuGenerationBatches` stores `PackedGpuGenerationTarget` (camera chunk, generated region window, view radius, source contract generation, and edit-store generation); when the target is unchanged and batches already exist, the main-world update skips repeated region planning/column-cache walking while still refreshing generated-region mask metrics. This lowers steady-camera CPU work and gives the next moving-window eviction/reuse step a clear target signature. Static proof: `cargo test -p rumpel_render packed_gpu_generation_target --lib` and `just verify`.
- 2026-06-02: Generated-region lifecycle metrics are now visible in the profiling contract. `profile sample` includes `packed_generated_regions_loaded`, `packed_generated_regions_active`, and `packed_generated_regions_visible`; `profile worst_packed` includes `generated_regions_loaded`, `generated_regions_active`, and `generated_regions_visible`; profile summaries print them, and visual/headless generated compare reports plus acceptance checks require positive loaded/active/visible regions for generated runs. This makes camera-driven generated window eviction/reuse measurable before changing lifecycle behavior. Static proof: `cargo fmt --check`, `bash -n` for compare/profile scripts, synthetic summary check, and `just verify`.
